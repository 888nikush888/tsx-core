# CCXT is the only exchange integration layer. CCXT Pro is bundled with the
# package and provides the private/public WebSocket watchers used by the
# executor. Runtime dependencies are fully resolved and hash-pinned below.
ccxt==4.5.75
