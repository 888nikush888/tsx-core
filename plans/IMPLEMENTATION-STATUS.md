# Umsetzung des verifizierten Audits

## Neuester Arbeitsstand: 4. September 2026, Schema 46 – Reparatur-Gesamtlauf grün, 009 in Einzelprüfung

Maßgeblicher Fortsetzungsstand; nachfolgende Abschnitte bleiben chronologische Teilnachweise. Weiterhin ausschließlich lokale Desktopkopie; keine Server-/Anwendungs-/Telegram-/Börsenkontakte, Trades, Commits, Pushes oder Releases.

**Aktuell:** Der letzte vollständige Python-Nachlauf bestand mit **479/479 Tests**; der letzte vollständige Node-Nachlauf bestand mit **198/198 Testdateien**. Frontend **23 Dateien / 136 Tests**, Backend-Typecheck, globaler ESLint/Ruff, Architektur (**166 Module / 762 Imports / 0 Zyklen**), Complexity (**0 Warnungen/Verletzungen; 3,56/15**) sowie Frontend-Lint und Produktionsbuild sind grün. Der frühere vollständige Modulratchet **43694 bestand mit 196/196 Testdateien**, **96,26 % Statements/Zeilen, 86,76 % Branches, 99,40 % Funktionen** innerhalb des unveränderten Budgets; diese Coveragewerte werden nicht rückwirkend auf den danach veränderten Quellstand übertragen. Alle vorherigen roten Läufe und Korrekturen bleiben unten dokumentiert. Dies ist der gemeinsame lokale Reparaturnachweis; 009 ist in Arbeit, die neue abschließende kritische Coverage/Mutation und vollständige 008-Abnahme folgen erst danach. Keine Grenze wurde gesenkt.

Der [begrenzte 005-Anschlusscheck](005-INTEGRATION-HANDOFF.md) fand keine weitere fehlende Consumer-Verkabelung im beauftragten Scope. Verbleibende Provider-/Historien-/Assetbelege bleiben ausdrücklich ungeklärt, nicht durch lokale Fixtures freigegeben. Neue Phase 009: vollständige Offlineinventur der gepinnten 103 CCXT-REST-IDs, gemeinsames Paritäts-/Reviewgate und produktgenaue Anbieterprüfung. Kein beliebiges Setzen von Zertifizierungsflags und keine echte Kontoabnahme.

### Jüngster 009-Nachweis

- Offlineinventur: alle **103 REST-/76 Pro-IDs**, SDK 4.5.75, echte Quell-/Methodenhashes. Nach dateigebundener semantischer Prüfung jetzt **1 existing / 0 pending / 66 not_easy / 36 not_derivative**; Inventurprüfung nach der letzten Entscheidung erneut Exit 0. Die 41 fehlenden Pflichtfähigkeiten sind von den konkret geprüften SDK-/Produkt-/Schutzgrenzen getrennt. Fehlende API-Zusicherungen wurden nicht als Fähigkeit erfunden.
- Neues gemeinsames Paritätsformat prüft zehn Klassen und 62 Einzelpflichten mit positiven/adversarialen Originalfixtures, Dateihashes und unabhängigen Erwartungsbindungen. Der Formatvalidator führt keine Artefaktbefehle aus und liefert bewusst noch keine Implementierungs- oder Providerfreigabe. Runtime- und Root-Buildprüfung sind eingebaut; tatsächlich ausgeführte konkrete Profilabnahmen und unabhängige Pins fehlen noch.
- Zusätzliche permanente echte SDK-Charakterisierungen: Bitget/BingX **5/5** sowie ApeX/Bybit-EU/Gate-EU/Extended/WEEX/Deepcoin **8/8**, zuletzt 1,959 s; Ruff grün. Abgefangene Requests und negative Identitäts-/Produkt-/Preisfälle, null Providerzugriff. Anfangs falsche Testannahmen wurden am Pin berichtigt, keine Produktionsschutzregel abgeschwächt.
- Einzelbelege in den bestehenden 009-Researchnotes, drei unabhängigen Kandidatengruppen und der finalen KuCoin-Futures-Nachprüfung. **Kein neues Profil freigegeben:** Alle zusätzlichen Derivatekandidaten überschreiten mindestens eine konkret belegte Grenze der vorhandenen kleinen Profil-/Provideradapter-Architektur. Bybit und Kraken Futures wurden ebenfalls korrekt von `existing` nach `not_easy` zurückgestuft; ausschließlich Hyperliquid bleibt im engen Master-Key-only-/USDC-Perp-Scope lokal implementierungsverifiziert. Providerabnahme und 008-Endabnahme bleiben getrennt.
- Hyperliquid bindet den Private Key jetzt vor jeder Clientkonstruktion kryptografisch an die konfigurierte Master-Wallet. Fremde, malformed und Null-Schlüssel scheitern ohne Secret-Ausgabe; Agent-Wallets bleiben ohne gebundene Grant-Beobachtung gesperrt. Der Runtime-/Root-Buildpfad akzeptiert genau den unabhängig gepinnten V2-Receipt und weist nicht freigegebene Profildeklarationen als `quarantined` aus, statt sie durch Legacy-Bools freizuschalten.
- Runtimebrücke: festes `ccxt_implementation_reviews.py`, aktuell **leer**, statt selbst behaupteter sieben Flags; exakte Receiptbytes, Scope, Profilhash sowie tatsächliche Executor- und SDK-Sourcebäume. Parent-/Ancestor-/Descriptorprüfung, begrenzte Bytes/Dateianzahlen, keine Links oder JSON-Duplikate. Zwei unabhängig reproduzierte Grenzfehler (widersprüchliches Profilmodell, String-Pin als Substringcontainer) korrigiert und gegengetestet. **12 Receipt- und 11 Registrytests grün**. Alte Profile sind jetzt nur in dieser lokalen Arbeitskopie quarantined; **nicht ausrollen**, solange unabhängige Scopeabnahme und Pins fehlen. Der bestehende Clientstatuscheck wirkt auch auf vorhandene Clients; dieser Zwischenstand ist keine betriebsfähige Migration.
- KuCoin: reiner neuer `kucoin_contracts.py`-Baustein, **13/13** Tests einschließlich tatsächlicher SDK-Signierung mit Integer-Contracts, beider IDs und LONG/SHORT. Keine Float-/Bruchmengenrundung, Roh-Decimalquelle getrennt vom SDK-Parser. Noch keine Shared-Hooks, keine Kapazitäts-/Historien-/Funding- oder Profilfreigabe. Binance/OKX erhalten zusätzlich **10 permanente SDK-/Identitäts-/Einheitentests**, grün. Alle drei neuen produktiven Pythonmodule sind in Sonar-Sources enthalten; keine ausgeführte Sonar-Cloudanalyse behauptet.
- Neuer Python-Gesamtlauf **35871: 419 Tests, 107,172 s, Exit 0** nach Sourcefreeze. Vorlauf 19913 hatte bereits vor Abschluss der KuCoin-Korrektur einen älteren Modulstand importiert und endete mit fünf Decimal-Overflowfehlern; er bleibt ein fehlgeschlagener Lauf und wurde vollständig wiederholt. Ruff inklusive C901<=15 für die neuen Module grün. Fokussierte Node-Parität/Acceptance/Katalog **3/3**, Supply-Chain **1/1**, ESLint/Diffcheck grün. Dies ist noch keine neue vollständige 008- oder Modulcoverageabnahme.
- `--verify-inventory` nach dem finalen KuCoin-Futures-Beleg erneut Exit 0; die Matrix hat keine ungeklärten Kandidaten mehr. `--verify-complete` bleibt bis zur echten gebundenen Implementierungsverifikation der drei vorhandenen Profile absichtlich rot. MyOKX, Aster und die übrigen früher offenen Kandidaten sind jetzt jeweils durch konkrete gehashte Ausschlussbelege statt durch bloße Flagabweichungen entschieden. [MyOKX-Folgebeleg](009-MYOKX-ASTER-FOLLOWUP.md), [KuCoin-Futures-Schlussprüfung](009-KUCOINFUTURES-INDEPENDENT-FINAL-REVIEW.md).
- Root-Buildvergleich und CI-Anschluss sind lokal implementiert: tatsächliche Code-/Test-/Fixture-/Lock-Bytebäume, feste Pythonprüfung, unabhängige Pins, Herkunftscommit getrennt vom heutigen HEAD. Echte Temp-Git-Gegenproben verhindern zirkuläre Receipt-Commits und Umleitung durch geerbte Git-Variablen. Der Containerjob benötigt nun `verify` und denselben Pflichtvergleich vor der Verpackung; neue Supply-Chain-Regression zuerst rot, danach grün. Root-Fokuslauf **53848: 6/6** (Bridge, Parität, Acceptance, Supply Chain, dynamischer Katalog, Testregistry) grün, einschließlich vollständigem positivem CLI-Test mit bytegleicher, unabhängiger temporärer SDK-Kopie sowie negativen Node-/SDK-Driftfällen. Das installierte Entwicklungs-SDK besitzt Hardlinks und erfüllt selbst noch nicht den Runtime-Dateivertrag. Keine tatsächliche CI-/Profil-/Providerfreigabe und keine produktiven Pins aus Testfreigaben.
- Bestandsprofilscope korrigiert: Resolverpräferenzen sind jetzt Settlement-Whitelist und Rangfolge. Fehlende oder widersprüchliche relevante Metadaten sind nicht fallbackfähig; bekannte ausgeschlossene Produkte bleiben davon getrennt. Profile bewusst auf Hyperliquid USDC, Bybit USDT/USDC und Kraken USD eingegrenzt, keine Änderung von Modi, Strategien, Leverage oder Konten. **14 Resolver- und 4 Profiltests** grün; gepinnte SDK-Parser samt gemischtem Hyperliquid-Inventar tatsächlich getestet. Native, vom SDK bereits ersetzte Metadaten und First-DEX-Beweise bleiben gesonderte Grenzen. [Enger Scope und Nachweise](009-NORMALIZED-SYMBOL-SCOPE.md).
- Nach zunächst zwei echten Fehlern im vollen Pythonlauf (Inventurdrift nach Profilscopeänderung und drei fehlende positive Kraken-Fixturefelder) beide Ursachen eng korrigiert. Alle **103 bisherigen Inventurentscheidungen und Evidenzpins unverändert erhalten**, nur aktuelle Profil-/Quellhashes regeneriert; `--verify-inventory` erneut Exit 0. Vollständig wiederholter Root-Lauf **34426: 437 Tests, 108,225 s, Exit 0**. Node-Fokus **59931: 7/7** zu CCXT, FX-Sizing, Entryfrist/-preisen/-constraints, Recovery und Fallback grün. Keine neue abschließende Coverage-/008-Abnahme daraus abgeleitet.
- Kraken-Nachprüfung belegt native Flex-USD-Summarywerte und fand eine echte Originaltext-ABA-/Parallelitätslücke des bisherigen Accountloghelpers. Der eng begrenzte requestlokale Capture-Fix ist umgesetzt: Originaltext wird vor dem CCXT-Floatparser task- und clientgebunden erfasst, strikt geparst und innerhalb desselben bestehenden Budgets verbraucht; kein `last_http_response`, kein Floatvergleich und kein Retryloop. **26 neue Capturetests und weitere 48 betroffene Tests** waren fokussiert grün; der anschließende vollständige Pythonlauf mit 479 Tests schließt den Fix ein. Der Hook sieht weiterhin die originale angefragte URL, nicht beweisbar die finale Redirect-/Peer-Origin. Flex-Walletscope, aktuelles Fundingaccrual und der unabhängige Protected-IOC-Vertrag sind dadurch nicht erledigt. [Quellen, Gegenproben und kleinster Anschluss](005-KRAKEN-REPORTING-FOLLOWUP.md). Bybit-Fillfinalität bleibt ebenfalls offen; keine vollständige Paritätsabnahme aus synthetischen Formatfixtures.
- Finaler Containervergleich ergänzt: argumentloser `verify_implementation_runtime.py` verlangt echte gültige Receipts für jedes nichtleere deklarierte Profilinventar. CI führt ihn im fertigen UID65532-Image ohne Netzwerk, Schreibzugriff, Quellmounts oder Approval-Umgebung aus; der separate Root-Bytevergleich bleibt vorher verpflichtend. **12 neue Runtime-Fälle** agentenseitig grün, darunter echter temporärer Hardlink und CLI-Subprozess; keine echte Container-/CI-Ausführung. Root hat den gesamten Helper und die Tests geprüft. Beide neuen Pythonmodule sind im Sonar-Quellscope enthalten.
- Root reparierte außerdem fehlende read-only Container-Testeingaben: Inventurwerkzeug, gebundene Inventur und gemeinsame Node-/Python-Orderfixture. Keine gebackenen Quellen oder Receipts werden übermountet, die vollständige Suite bleibt erhalten. Supply-Chain-Regression zunächst rot, anschließend grün; ESLint grün. Drei Negativteststellen wurden von der aktuellen leeren Produktionsfreigabeliste entkoppelt: ausschließlich explizite eigene leere/Legacy-Testeingaben, keine bedingten Skips. So führt eine später legitime positive Freigabe nicht selbst zu einer zwingend roten Suite. Root-Nachlauf der Certification-/Registrydateien **12 + 11 Tests grün**, Ruff grün.

Die folgenden Punkte dieses Abschnitts sind chronologische Reparaturnachweise. Frühere Meldungen über offene Modulratchets werden durch den oben tatsächlich grünen Lauf 43694 ersetzt; spätere 009-Änderungen benötigen erneut ihre eigenen vollständigen Gates.

- M43-Originalbelege und M44-Recovery-Schedule sind im tatsächlichen lokalen Adapter-/Persistenz-/Runtimepfad verbunden: gemeinsames Fünferbudget, feste faire Phasen, dauerhafte Attempts/Leases, keine SDK-Autopagination oder versteckten Retries. Lokale Port-0-Transporttests belegen einzelne 502-/Timeoutversuche, `calls=null`, keine erfundenen Sourcefortschritte und vollständigen Transaktionsrollback. All-zero pausiert Zusatzreads, nicht Current-State-/Schutzreads.
- M45 ergänzt getrennte unveränderliche exakte FX-Ereignisbewertungen, native/FX-Exklusivität, Account-FKs, faire Bewertungsarbeit und zwei nullable Projektionsfelder. Varianten derselben ursprünglichen Quote stoßen betroffene Projektionen erneut an. Keine späteren Preise überschreiben historische Bewertungen oder Native-Originale. Echte 44→45-/43→aktuell-Migrationen, Backup-Restore und frühe/späte DDL-Fehlerrollback grün; alte Downgrade-Testfixtures wurden konsistent erweitert.
- `MoneyValue` hält einzelne Werte exakt rational, bei bewiesenem Aggregationsgrößenlimit konservative Grenzen. Decimalalias ist nur bei exakter Dezimaldarstellung gesetzt. Aktuelle Risiko-Reserven, Sizing und V2-Tierbudgets sind inzwischen angeschlossen: Reporting-/Settlement-Einheiten bleiben getrennt; Admission liest die Originalbelege erneut, ein synchroner Zeitfence liegt unmittelbar vor Dispatch. FX-Fehler nach möglichem Versand bleiben `unknown`; vor Versand blockieren sie ohne künstlichen Unklarheitsincident. 19 echte Node→Python→CCXT-Signing-Fakes sowie tatsächliche journalisierte Nichtversand-/Ablaufgegenproben sind grün.
- Begrenzte automatische Geldbewertung ist nun an die vorhandene Fundingaufnahme angeschlossen, ausschließlich aus passenden gespeicherten Originalen, ohne zusätzliche Börsenabfrage. Späte Gebühren invalidieren ältere Freigabebelege. Historische Ereignisse ohne passende Kurse bleiben ungeklärt; USD, USDT und USDC werden nie pauschal gleichgesetzt. Bybits offene History-/Optionscope-Grenze bleibt ausdrücklich gesperrt und wurde für positive Tests nicht umgangen.
- Fillprojektion, Analytics, Journal, Viewer, Telegram und das Abschlussereignis übertragen exakte Werte, Währung und Bewertungsstatus. Die Oberfläche zeigt rationale Näherungen ausdrücklich als Anzeige, exakte Brüche in Details, Grenzen als Grenzen und ungeklärte Werte nicht als Null. Charts trennen Währungen. Ein echter M36-Regressionsbefund ist repariert: ursprüngliches Legacy-PnL bleibt im ersten unveränderlichen Prüfbeleg erhalten, ohne in die neue Wirtschaftsrechnung einzufließen.
- M46 erhält alte adaptive Bewertungen bytegenau und ergänzt nullable exakte Werte und Quellen-/Invalidierungsbelege. Migration, Backup, Wiederanlauf und frühe/späte Rollbacks sind fokussiert grün. Die adaptive Consumer-/Cacheintegration ist nun ebenfalls fokussiert grün: neun Fallgruppen, native Kleinstverluste, rationale Schwellen, echte begrenzte Großaggregation, Konto-/Währungsbindung, Restart, spätere Quote, Konflikt und verspätete Gebühren. Alte Originalbewertungen bleiben unverändert; ungültige Quellen sperren Automatic bis zur expliziten neuen Policy. Fixed/Locked/Shadow bleiben erhalten. Core-/Analytics-Regressionsdateien, Typecheck, Lint und Architektur grün.
- Tatsächlicher Python-Gesamtlauf mit Coverage (Handle 45895): **357 Tests, 57,953 Sekunden, Exit 0**. Ausschließlich Produktionsmodule: **86 %**; FX-Producer und Recovery-Schedule jeweils **99 %**, neuer Tierbudgethelper **100 %**. XML aktualisiert. Frontend vollständig **23 Dateien / 133 Tests**, 4,66 Sekunden und Lint grün. Letzte Architekturprüfung: **166 Module / 762 Imports / 0 Zyklen**; fokussierte Node-Nachläufe, Typecheck und betroffener Lint grün.
- Vollständiger Node-Modulratchet bleibt offen: die beiden 180-Sekunden-Timeouts bleiben echte fehlgeschlagene Gates. 24 separat auf Prozess-/Datei-/Portisolation geprüfte neue Tests sind jetzt explizit parallel freigegeben (191 insgesamt); Workerzahl, 180-Sekunden-Budget und Coveragegrenzen unverändert, vier serielle Barrieren erhalten. Neuer Gesamtlauf folgt nach Source-Freeze. Ältere Mutation-/Playwright-/kritische Coveragewerte sind keine Abnahme des neuen Geldpfads. **005 bleibt offen, danach 009 vor finaler 008; keine CI-/Sonar-/Provider-/Releasefreigabe.**

### Jüngster gemeinsamer Prüfnachlauf

- Unabhängiger Oberflächenreview reproduzierte zwei echte Fehler an ungültigen DTO-Grenzen: `not_proven` wurde im Chart trotz ungeklärter Textanzeige berücksichtigt; ein widersprüchlicher Decimalalias konnte eine negative Fraktion als Null anzeigen. Neue Tests zunächst rot, danach grün: Chart verlangt nun `complete`, und Decimalalias/Fraktion/Grenzen werden mit begrenzter Ganzzahlarithmetik auf Konsistenz geprüft. Frontend **23 Dateien / 135 Tests, 4,57 Sekunden**, Typecheck und Lint grün. Der lokale Browser-Regressionslauf mit vollständiger Mock-API bestand **52/52**, je 13 Chromium/Firefox/WebKit/Mobil; der eigene Vite wurde anschließend beendet. Der DTO-Fix folgte zum Ende dieses Browserlaufs; seine neuen Grenzfälle sind durch die danach grüne Einheitensuite belegt, nicht rückwirkend durch ältere Browserfälle.
- Erster tatsächlicher neuer Modulratchet (Handle 9663) brach bei `test_trading_order_migration.js` ab: dessen alte Testdatenbank enthält `trading_channel_risk_evaluations` nicht, M46 verweigert die Migration korrekt. Dieser Fixturefehler wird geprüft; die Produktionsmigration wird nicht zum Verbergen fehlender Originaltabellen gelockert. Die anschließend erzeugte Teilcoverage ist kein Gesamtnachweis.
- Dieser Fixturefehler ist inzwischen korrigiert: ausschließlich `tests/fixtures/legacy_ingress_schema.js` und `tests/test_trading_order_migration.js`; echte alte M9-/M16-Tabellen statt erfundener M46-Provenienz. Vier reduzierte Legacy-Fixtures plus M45/M46, Originalwerte, Konfliktrollback und Reopen bestanden (sechs Dateien), Produktionsmigration unverändert.
- Der nächste tatsächliche Gesamtlauf (40600) erreichte die Repositoryprüfung und scheiterte an der versehentlich vom Windows-Python-Starter erzeugten zusätzlichen Installation unter `tsx-core/Python` (Erzeugungszeit 03.09., 00:41:32). Der zuvor gemeldete Starteraufruf war keine beabsichtigte Abhängigkeitserweiterung. Nach Prüfung des exakten Pfads, Standardinstallationsinhalts, fehlender aktiver Prozesse und des freien Zielpfads wurde **nur dieser neue Ordner wiederherstellbar verschoben**, nicht gelöscht: `C:/Users/nikla/AppData/Local/tsx-core-dev-runtime/recovered-unintended-python-20260903`. Gepinnte Python3.12-Umgebung, Projektcode und Benutzerkonfiguration unverändert. Repository-Governance danach grün; keine Encoding-Ausnahme/Ignore-Regel hinzugefügt. Erneuter vollständiger Modulratchet läuft als 43694.

Stand: 2026-09-02. Lokale Arbeitskopie `C:/Users/nikla/Desktop/tsx-core`, Basis `f8089277d0667dd45bcf070037800d423fd89254`. Umsetzung läuft; **nicht vollständig und nicht zur Ausrollung freigegeben**.

## Aktuellster Integrationsnachlauf (2. September, Schema 42)

Die folgenden konkreten Ergebnisse ersetzen keine Gesamtfreigabe; ältere Abschnitte darunter sind chronologische Teilstände.

- Zwei vollständige Node-22-Läufe mit **161/161 Testdateien** bestanden: kritische Coverage **96,93 % Zeilen / 90,15 % Branches / 100 % Funktionen**, alle acht unveränderten Einzeldateigates; anschließend der tatsächliche vollständige Modulratchet **96,09 % Zeilen/Statements, 85,70 % Branches, 99,35 % Funktionen**. Nicht nur der Ratchet-Selbsttest wurde ausgeführt. Die bisherigen Grenzen 95,01/83,33/99,09/95,01 bleiben unverändert; die gemessene Verbesserung muss beim endgültigen plattformübergreifenden Abschluss berücksichtigt werden.
- Tatsächlicher Python-Gesamtlauf mit Coverage nach TTL-/SDK-/Fillidentityintegration: **276 Tests in 34,207 Sekunden, Exit 0**. Ausschließlich produktiver Executorcode erreicht **84 %**, neue TTL- und Fillidentitymodule jeweils 100 %; keine Testmodule als Produktionscoverage ausgegeben. Danach ergänzte Kraken-Matrix: **7/7** beim Root nochmals grün, ohne Fähigkeit freizuschalten.
- Echte lokale Playwright-Prüfung: **52/52**, je 13 Chromium/Firefox/WebKit/Mobile-Chromium; Mock-API, Tastatur/Reduced-motion/WCAG, eigener überprüfter Node-22-Vite anschließend beendet. Duplikate 0,75 % bei unveränderter 5%-Grenze; Lizenz-/Dependency-/Buildcontext-/Release-Artefakt-/Frontend-Erreichbarkeits-/Risikoaktenchecks grün. Das ist kein veröffentlichtes Release und keine Providerabnahme.
- **Nach den zwei Node-Gesamtläufen gefundener und korrigierter Fehler:** Ein echtes `ENTRY_INTENT_EXPIRED` nach Adapterübergabe erzeugte eine `unresolved`-Operation, aber einen terminalen `blocked`-Intent. Die Klassifikation prüft nun zuerst den eigenen dauerhaften Dispatchzustand: kein terminales Blockieren oder Fallback bei ungeklärter Übergabe; gemeinsamer Unknown-/Isolationpfad, ursprünglicher Fehlercode bleibt lesbar. Gegenprobe vor echtem Dispatch bleibt lokal terminal; Neustart wiederholt den unklaren Send nicht. Acht Fokusdateien, TSC, ESLint und Complexity grün. HTTP-422-`ENTRY_*` allein bleibt ausdrücklich kein Nichtversandbeweis; lokale spätere Tokenwartezeit wird nicht durch einen erfundenen Providerbeleg freigegeben.
- **Weiterer echter Wiederanlauffehler korrigiert:** `recoverPreparedExits` ignorierte ACK-/Phasengeschichte und konnte widersprüchliche SL-/TP-/Flatten-Vorbereitungen erneut submit-ready setzen. Neuer permanenter Test mit **51 Fällen**, davon zuvor 30 rote unzulässige Resets, jetzt grün: ursprüngliches `created`, evidenzfreies `prepared/0` beziehungsweise `abandoned/1–2`; ACK, verbrauchte Phase, ursprüngliche Remote-ID/Aktivstatus, lokaler Response oder Fillpreis erlauben keinen Reset. Journaloriginale bleiben unverändert. Bestehende echte TP-/Emergency-Hard-Crash- sowie Cancel-/Protectiontests ebenfalls grün.
- Die durch diese spätere Sourceänderung überholten Gesamtläufe werden nicht als Endabnahme ausgegeben. Ein isolierter Lifecycle-Test-Zeitrace (`since=Date.now()` nach bereits erfasstem Abschluss) wurde auf den erfassten Quellenabschluss korrigiert; derselbe negative fehlende-Historienfenster-Fall bleibt erhalten. Eine unsichere optionale Testzugriffskette im Frontend wurde explizit geprüft; 12/12 zugehörige DOM-Tests und warnungsfreier Lint grün.
- Der durch die [001–003-Restmatrix](001-003-REQUIREMENTS-RECONCILIATION.md) belegte globale Worker-Fairnessfehler ist jetzt ebenfalls lokal korrigiert: stabile `(created_at,id)`-Rotation mit maximal 100 tatsächlichen Versuchen pro Durchlauf, Rückkehr zum Anfang nach Seitenende, kein Überspringen unversuchter Zeilen bei Pause. Sechs permanente Szenariogruppen inklusive echtem zulässigem Paper-Entry hinter 100 unveränderten unaufklärbaren `submitting`-Altintents, Restart, Löschung und gleichem Zeitstempel grün. Kein neuer Datenbankcursor als Wirtschaftsbeleg und keine Migration. Beide neuen Node-Dateien sind registriert; Root-Nachlauf mit RecoveryWorker/ControlRaces grün. Der eigenständige [004-Abgleich](004-REQUIREMENTS-REVIEW.md) und die Kraken-Matrix unterscheiden lokale Code-/Serialisierungsbelege weiter von nicht belegter echter Providerfähigkeit.
- Erzwungene Mutationstests ergaben Queue 72,67 %, Retry 71,43 %, Schema 77,02 %, aber zunächst Trading-Risk 53,53 % (rot). Der Test wurde fachlich erweitert; zugleich wurde belegt, dass Stryker `--force` allein historische, inzwischen außerhalb des Bereichs liegende Mutanten im Report ließ und die alten Zeilengrenzen den eigentlichen Sizingkern nicht mehr erreichten. Der Risikoshard prüft jetzt die gesamte Datei, ein Forcedlauf deaktiviert Incremental explizit. Ein tatsächlicher vollständiger frischer Risikolauf läuft noch; keine Gesamtfreigabe aus dem bereits grünen engen Nachlauf.
- Das tatsächliche promtool/amtool-Gate ist jetzt grün: 18 Regeln, Regeltests und beide Konfigurationen. Isoliert bezogene Stock-Windowsvalidatoren entsprechen den offiziellen Versionen/Hashes, sind aber kein gehärteter Release-Buildnachweis. Tatsächliche SBOM-Erstellung mit separat signaturgeprüftem npm 10.9.2 grün: Backend 119/Frontend 60 Komponenten, CycloneDX 1.5, Paket-/Lockdateien unverändert. Deployment-Image-Prüfung bleibt ohne die drei erforderlichen Digestvariablen rot; keine Platzhalter.
- AUX-01 besitzt jetzt einen kombinierten positiven und negativen Nachweis im echten CredentialStore-/Baseline-/Drainpfad: klassifizierte terminale Historie erlaubt Rotation, eine neue offene Order blockiert vor Kandidatenübernahme. Originalcredentials bleiben im negativen Test bytegleich; der künstlich im Speicher wieder eingesetzte alte `orders.length`-Fehler wird vom positiven Test erkannt. Sechs Fokusdateien grün, keine Produktionsänderung dafür.
- M42 implementiert die ausdrücklich freigegebene additive [Mengen-Normalisierungsbeobachtung](005-KRAKEN-QUANTITY-NODE-NOTES.md): strenger Node-/Python-Vertrag, immutable echte Fill-/Account-/Generations-/Readbindung, keine erfundenen Altbelege und keine Nicht-1-Cashlegfreigabe. Vier neue Node-Dateien einschließlich sieben tatsächlicher Python→Node-Fakefälle, echter Schema41→42-Migration, Backup/Restart/Rollback grün. Python-Agent meldet neuen Gesamtlauf **297/297**, ohne alte Coveragezahl als neue Abdeckung auszugeben. Node-Gesamtabnahme muss nach diesen Änderungen neu erfolgen.
- Root hat den neuen Python-Gesamtlauf mit Coverage inzwischen selbst ausgeführt: **297 Tests in 45,263 Sekunden, Exit 0**, ausschließlich produktiver Code **84 %**, neue Quantity-Provenienz **98 %**; XML-Artefakt aktualisiert. Echter lokaler promtool/amtool-Nachlauf ebenfalls Exit 0. Ein zunächst ohne lokale Werkzeugpfade aufgerufener Monitoringcheck scheiterte korrekt an fehlendem Docker-Daemon; es wurde kein Docker-Daemon gestartet oder gehärteter Build behauptet.
- Neuer vollständiger Node-22-Nachlauf unter M42: **168/168 Testdateien, Exit 0**; kritische Coverage **97,05 % Zeilen/Statements, 90,84 % Branches, 100 % Funktionen**, alle acht unveränderten Einzeldateigates bestanden. Die neue Mengenaufnahme enthält auch die permanenten UID-/Mode-/Fingerprint-Gegenprüfungen und akzeptiert semantisch gleiche `1`/`1.0`, ohne Originaltexte umzuschreiben. Der Modulratchet muss für diesen Folgestand noch tatsächlich ausgeführt werden; sein bisheriges 180-Sekunden-Budget bleibt unverändert.
- Erster tatsächlich **frischer Voll-Risikoshard** (gesamte `src/trading_risk.ts`, keine alte Zeilengrenze) bestanden: **642 aktuelle Mutanten, 546 killed + 2 timeout, 94 survived, 85,36 %, Exit 0**, 4m38s. Historische Incrementalartefakte blieben unangetastet und flossen nicht in den neuen Bericht ein. Der CI-Mutationsjob erzwingt denselben Freshmodus, lädt keine alten Mutanten mehr und veröffentlicht nur den aktuellen Bericht; Runner-/Workflowtest zeigte zuvor echtes Rot und ist jetzt grün. Noch gezielte zusätzliche Assertions für sicherheitsrelevante überlebende Orderleg-/Margin-/Tiergrenzen in Arbeit, keine Jagd auf äquivalente Textmutanten und keine Änderung der 70%-Grenze.
- Offen bleiben unter anderem die ausdrücklich angefragte FX-Zeitpolicy, der dokumentierte Kraken-Einheiten-/Batchvertrag, weitere echte Provider-/Revisionsnachweise sowie **009 vor finaler 008**. Lokal wird weitergearbeitet; keine Server-/Anwendungs-/Börsen-/Telegramverbindung, keine echten Trades, kein Commit/Push/Release.

### Nachlauf: Modulprüfung und frische Mutation

- Der tatsächliche M42-Modulratchet (Handle 12012) endete mit **Exit 1 / ETIMEDOUT** am unveränderten 180-Sekunden-Limit. Der vorige grüne kritische Gesamtlauf ersetzt diesen fehlenden Modulnachweis nicht. Eine anschließende Prozessprüfung fand keine weiterlaufenden eigenen Node-/c8-Testkinder; fremde Codex-Prozesse wurden nicht beendet. Laufzeitursache und sichere Beschleunigung werden geprüft, keine Tests oder Coveragegrenzen entfallen.
- Der zweite frische Voll-Risikoshard wurde nach Ende dieser Modulprüfung gestartet (Handle 18378, 22:49:35 lokal): erneut 642 aktuelle Mutanten, initialer Core-/Leverage-Tier-Test grün. Ergebnis noch offen; weder der Start noch ein früherer Report ist dessen Abschlussnachweis.
- Die neue tatsächliche Python→Node-Fixture läuft auch in den vorgesehenen CI-Umgebungen gegen zuvor installierte gepinnte Python-Abhängigkeiten: sowohl Verify als auch Sonar installieren Python 3.12 und die hashgebundenen Executor-Lockdateien vor dem vollständigen JavaScript-Lauf. Dies ist eine Prüfung des Workflow-Vertrags, kein ausgeführter CI-/Sonar-Lauf des uncommitteten Standes.

### Neueste Fortsetzung: freigegebene Stablecoin-FX-Methode

- Der Nutzer hat die zuvor offene Zeitpolicy delegiert und USD/USDT/USDC als Umfang genannt. Entscheidung samt Grenzen ist oben im [FX-Folgeplan](005-FX-FOLLOWUP-PLAN.md) festgehalten; frühere Hinweise auf diese noch fehlende Antwort sind historische Teilstände. Keine feste Parität zwischen den drei Währungen, keine neue Konto-Reportingkonfiguration.
- Der zweite vollständige frische Risikoshard 18378 ist tatsächlich **Exit 0**: 642 Mutanten, 596 killed + 2 timeout + 44 survived = **93,15 %**, Laufzeit 4m33s. Die Risikodatei blieb unverändert; keine veralteten Incrementalberichte einbezogen. Verbleibende Mutanten werden nicht pauschal als äquivalent ausgegeben.
- Neuer reiner FX-Python-Producer samt begrenzten originalen Einzelreceipts: 22 neue Tests; agentenseitig gesamter Pythonlauf **319/319, 42,484 s, Exit 0**. Keine neue Coveragezahl daraus abgeleitet. SDK-interne versteckte Leseretrys wurden durch eine zunächst rote echte Transportprobe erkannt und für genau diese neuen Reads unterbunden; ursprünglicher Fünferhaushalt unverändert.
- Root hat exakte Rationalrechnung, strengen Node-Receiptvertrag, kohärente As-of-Ableitung und tatsächlichen Python→Node-Test lokal ergänzt; Fokus, TSC, ESLint, Architektur und Complexity grün. [Node-Teilstand und offene Integration](005-FX-NODE-NOTES.md). Keine produktiven Quotes gespeichert oder Accounting-/Entrygates freigegeben.
- Der vollständige **171-Dateien-Modulratchet 98309** mit Zwei-Prozess-Opt-in endete tatsächlich mit **ETIMEDOUT nach 180 Sekunden**, zuletzt nach bestandenem Frontend-Bundle-Test; keine Gesamtfreigabe. Read-only-Nachprüfung fand keine verbliebenen eigenen Testkinder. Nach konkretem Isolationsreview jetzt 167 explizite Freigaben und vier exklusive Barrieren; nur der Modulratchet erhält vier Worker. Fokusregressionen und Lint sind grün, ein neuer schwerer Gesamtlauf steht aus. Grenzen und Coverage-Baseline unverändert.
- **M43 lokal ergänzt:** unveränderliche, verifizierten Konten/Fingerprints/Credentialgenerationen und tatsächlichem Profil zugeordnete FX-Originale; Conversions referenzieren ihre Originalbelege und werden beim Lesen daraus erneut berechnet. Echte Schema-42-zu-43-Migration, fehlgeschlagene Migration/Rollback, verlustfreies Backup/Restart und unveränderte alte Geldbuchungen geprüft. Fokustests grün; keine produktive Quote-Abfrage, Geldbewertung oder Entry-Freigabe angeschlossen. Scheduling und monetäre Consumer bleiben noch offen.

## Nachgewiesene Fortschritte in 001

- Gemeinsame Konto-Mutationsqueue mit explizitem Reentranzkontext, sofortigem Entry-Fence und überlappenden Sperrhaltern. Vorbereitete Entries bleiben nach Stop/Start widerrufen. Andere Konten bleiben unabhängig.
- Letzte Entry-Prüfung unmittelbar vor Dispatch: Runtime, Konto, Strategie, ursprüngliche TTL, Identität und Credential-Generation; zusätzlich aktuelle Routen-/Schemafreigabe, Kontolimit, unbekannte andere Orders und offene Incidents. Die eigene Planreservierung wird nicht als fremde Position gezählt. Gepinnte Workflows werden nicht gegen neue Revisionen ausgetauscht.
- Migration 25: Versionszähler, CAS-Persistenz, terminale Intent-Sperre, beweisbarer Provider-Symbol-/Exchange-ID-Schlüssel mit eindeutigem Index. Keine Altzeilen gelöscht.
- Node-Antwortvalidierung gegen angefragte Client-ID, bekannte Exchange-ID, Mengen, Dezimalfelder, Zeitgrenzen und Symbole. Kumulative Ordermenge monoton; verspätete Evidenz reaktiviert terminale Orders nicht.
- Python: keine erfundenen Exchange-IDs, Protected-Batches nach exakter Client-ID statt Arrayposition. Unvollständige Antworten liefern ausdrücklich ungeklärte Ergebnisse; unabhängige bestätigte Legs bleiben beim Transport und in SQLite erhalten. Neue Entries des betroffenen Kontos werden gesperrt.
- Verifikation persistiert eine nicht-geheime Credential-Generation. Alle Submit-/Protected-/Cancel-Requests benötigen beide verifizierten Bindungen. Python hält einen Konto-Client-Lease über die gesamte Mutation und prüft die Bindung vor Hebeländerung/Create/Cancel erneut. Clientwechsel wartet, andere Konten nicht.
- Cancel erhält bekannte Exchange-ID und Providersymbol. Widersprüchliche Client-/Remote-IDs oder Symbolkontexte werden vor dem Cancel abgewiesen; fehlende Cancel-Ausführungsmenge bedeutet ungeklärt, nicht Null.
- Aus Plan 002 sicherheitsbedingt vorgezogen: Der Python-Fehlerpfad nach unklarem Batch führt keinen symbolweiten unbewiesenen Flatten mehr aus. Das ersetzt **noch nicht** die geplante Ownership-/Recovery-State-Machine.
- Aus AUX-01 vorgezogen: Credential-Wartung prüft aktive Orders statt sämtliche terminale Historie; alte Bindung wird vor Cancels verifiziert.
- Echte Runtime-/Control-Races: gültiger Start trotz Startup-Entry-Hold; während Freigabe verlangter Stop oder erneute Kontosperre gewinnt gegen die ältere Freigabe. Beide Tests registriert.
- Gemeinsame Node-/Python-Evidenz-Fixture; fehlende Remote-Fillmenge bleibt nullable, fehlende Mengenbestätigung kann keinen belegten Durchschnittspreis ersetzen. Dezimal-Grammatik in Python begrenzt und an der Boundary strikt.
- Scoped Exchange-ID-/Provider-Symbol-Korrelation; keine Stop-Übernahme durch bloße Preisähnlichkeit. Migrationskonflikte vor Indexanlage vollständig diagnostiziert; Rollback und wiederholtes Upgrade geprüft.
- Bybits UTA-Cancel-Pfad verwendet jetzt dieselbe belegte aktive/terminale Einzelorder-Abfrage wie Recovery statt des generischen `fetch_order`. Bereits terminale Orders werden nicht erneut storniert; abweichende IDs/Symbole und fehlende kumulative Ausführung bleiben ungeklärt. Drei zusätzliche profilübergreifende Python-Tests, zunächst rot und nach Reparatur grün.

## Nachgewiesene Fortschritte in 002

- Migration 26 legt dauerhafte Remote-Evidenz an; keine historischen Daten gelöscht. Unbekannte Orders/Fills behalten erste/letzte Beobachtung, Wiederholungszahl, Provider-Identität und begrenzte ökonomische Payload ohne Raw-Header/Credentials.
- Fills ohne echte Trade-/Order-ID oder Providerzeit werden in Python nicht mehr verworfen oder mit erfundenen IDs versehen, sondern als ungeklärte Ereignisse übertragen.
- Auch nichtleere unbekannte Client-IDs bleiben in SQLite sichtbar und sperren Neueinstiege. Leere Folgesnapshots und Neustarts löschen diese Ungewissheit nicht. Eindeutig nachgewiesene spätere Orderkorrelation kann passende ungeklärte Fills als verwaltet klassifizieren.
- Fill-Persistenz prüft bestehende Provider-IDs auf geänderte ökonomische Inhalte statt `INSERT OR IGNORE`; Konflikte erhalten beide Belege, alter Ledgerwert bleibt unangetastet. Unterschiedliche reale Fill-IDs bei gleicher Zeit/Menge bleiben getrennt.
- Eigene Nettomenge wird vor Positions-/Schutzänderungen aus Entry-/Exit-Fills rekonstruiert und gegen kumulative Orderausführung sowie Remote-Position abgeglichen. Manuelle Same-side-Mehrmengen werden nicht übernommen, durch größere Stops vereinnahmt oder automatisch geflattet. Fehlende Mengenbelege bleiben fail-closed.
- Paper-Snapshots liefern jetzt ebenfalls belegten Symbolkontext. Eine alte Partial-Fill-Fixture enthält nun die zu ihrer bestätigten Ausführung gehörenden Fills; Schutz-/TP-Erwartungen unverändert.
- Migration 27 ergänzt das dauerhafte Operationsjournal und persistierte Ersatzorder-Generationen. Geschützte Entries, sonstige Engine-Submits und Cancels schreiben ihre Phase vor dem Börsenaufruf. Ein verlorenes Ergebnis bleibt ungeklärt; das Journal verhindert blindes Wiederholen. Eine leere Remote-Liste oder ein Cancel mit weiterhin offenem Status gilt nicht als Auflösung.
- Journal-Tests prüfen Vorbereitung, finalen Fence, bestätigte Ergebnisse, Timeout, Datenbank-Schließen/erneutes Öffnen sowie exakte terminale Cancel-Evidenz. Das ist noch **kein** harter Prozessabbruchtest an jeder Phase und noch kein vollständiger Recovery-Worker.
- Nachweislich nie gesendete `planned`-/`submitting`-Pläne können bei unverändertem Plan und aktuellen Freigaben mit denselben IDs fortgesetzt werden. Der originale Erstellungszeitpunkt und die ursprüngliche TTL bleiben erhalten. Veränderte Orderzeilen oder mögliche Dispatches verhindern diesen Wiederanlauf.
- Abgelaufene, deaktivierte oder nicht mehr passende, sicher ungesendete Pläne schließen ihre Nullpositions-Reservierung und verwerfen die vorbereiteten lokalen Orders ohne Börsen-Cancel. Bei fachlicher Ablehnung werden Freigabe der Reservierung und Intent-Status transaktional gespeichert. Unsichere Versandzustände werden dabei nicht als ungesendet behandelt.
- Aus 003 vorgezogen: persistierte Generationen für Stop-/TP-Ersatz und Flatten; erneute Vorbereitung derselben noch ungesendeten Ersatzorder behält ihre ID. Eine nachweislich abgewiesene Flatten-Order wird nicht unter derselben Client-ID erneut benutzt. Vollständige Lifecycle- und Generations-Absturztests bleiben offen.
- Migration 28 ergänzt Quellen-/Abfragefenster-Nachweise und persistierte Zeitpunkte gezielter Order-Nachprüfungen. Node sendet lokale offene/ungeklärte Verpflichtungen einschließlich alter Orders an den Executor; die Auswahl ist begrenzt und rotiert nach zuletzt bearbeiteter Anfrage. Negative Abfragen terminalisieren keine Order. Quellenmetadaten werden bereinigt und erst nach Aufnahme der ökonomischen Ereignisse gespeichert; verschobene Abfragen werden nicht als bearbeitet markiert.
- `history_reader.py` ist an Open-State angebunden: exakte Exchange-ID-Abfragen für die vorhandenen Profile, Bybit getrennt aktive/terminale Orders, Hyperliquid unterstützt den Client-ID-Abfrageparameter. Für Kraken bleibt ein nicht nachgewiesener Client-only-Pfad ausdrücklich `unsupported`. Lokale Verpflichtungen ergänzen Krakens Symbolliste auch bei leeren Remote-Listen. Die 45-Tage-Fixture geht nicht durch das alte 30-Tage-Fenster verloren.
- Höchstens fünf zusätzliche gezielte Abfragen je Durchlauf, mindestens eine Sekunde Antwortreserve; 429/Transportfehler unterbrechen weitere Zusatzabfragen desselben Durchlaufs. Historienarbeit wird pro Provider/Umgebung serialisiert, andere Provider bleiben unabhängig. Das ist **noch nicht** der vollständige persistierte Retry-After-/Backfill-Cursor-Mechanismus.
- Erste profilrichtige Listenabfragen: Bybit hält aktuelle History-/Fill-Limits und ein maximal siebentägiges jüngstes Fenster ein; Hyperliquid/Kraken bekommen keine zusätzliche 500-/1000-Abschneidung accountweiter CCXT-Listen. Krakens `fetchOrders` wird nicht mehr als ungezielter History-Endpunkt missbraucht. Diese Listen bleiben im neuen Quellenvertrag ausdrücklich `unknown`, bis Cursor-/Vollständigkeitsbeweise ergänzt sind. Die Quellenmetadaten sind noch **kein vollständiger Closure-/Release-SafetyProof**.
- Gemeinsame Python-Vorprüfung: fehlende/ungültige/negative Positionsmenge wird nicht als Null behandelt. Offene Fremdorders am Zielsymbol blockieren den Entry vor Hebeländerung oder Batch-Erstellung. Für Hyperliquid, Bybit und Kraken Futures lokal getestet.
- Migration 29 und `trading_history_repository.ts`: dauerhafte Fortsetzungspunkte pro Kontoidentität, Quelle und Symbolscope. Antwort und Checkpoint sind revisionsgebunden; Fortschritt wird erst nach Speicherung der Orders/Fills/ungeklärten Ereignisse übernommen. Ein später CAS-Konflikt rollt alle Checkpoints und den Quellenbeleg derselben Aufnahme zurück. Wiederholung einer alten Antwort kann keinen Cursor zurücksetzen. Neu entdeckte ältere Verpflichtungen starten ein ausdrücklich ungeklärtes größeres Zeitfenster und invalidieren ältere Antworten.
- `history_pagination.py` hängt am echten Open-State-Pfad: Bybit-Linearkontrakte mit unverändertem Response-Envelope, maximal siebentägigen Fenstern, explizitem Cursor auch bei leeren Zwischenantworten und wiederholbarer Überlappung; getesteter Abruf von jeweils 1.205 Orders und Fills. Historische Orders bleiben wegen der kürzeren Aufbewahrung ungefüllter Orders ausdrücklich `unknown`. Der reale CCXT-Parser wird mit belegtem Kontraktmarkt aufgerufen, damit fehlende optionale Felder keine falsche Spot-Zuordnung auslösen. Bybits aktuelle Open-Orders-Seitengröße ist auf das dokumentierte Maximum 50 korrigiert.
- Hyperliquid-Fills werden über den CCXT-Info-Aufruf inklusive unverändertem Zeitfenster, inklusiver Zeitüberlappung und ausgeschalteter Aggregation gelesen. Auch kurze Seiten werden fortgesetzt. Eine volle Seite am identischen Millisekunden-Zeitpunkt bleibt ungeklärt, statt Ausführungen mit `timestamp + 1` zu überspringen. Das dokumentierte Retentionslimit bleibt ausdrücklich sichtbar: Traversierung ist kein Beweis älterer Vollständigkeit. Hyperliquid-Orderhistorie ohne belegte Pagination wird nicht als vollständig ausgegeben.
- Gezielte Nachfragen und Backfill teilen sich insgesamt fünf zusätzliche SDK-Aufrufe pro Durchlauf. Der echte Adapterpfad testet zwei gezielte Bybit-Abfragen plus genau drei Seiten. Persistierte Retry-After-/Timeout-Wartezeiten erhalten den Cursor; Zahlen- und HTTP-Datum-Formate werden gelesen. Frische Positionen/Orders/Fills stehen weiterhin vor Backfill. Ein globaler, dauerhaft geteilter Cooldown über mehrere Konten/Prozesse ist damit noch nicht umgesetzt.
- Auch unzugeordnete terminale historische Orders werden nun vor Cursorfortschritt redigiert und dauerhaft aufbewahrt. Ihr Status allein beweist weder TSX-Ownership noch eine externe Konto-Baseline. Die neue Regression war zunächst rot und ist nach Reparatur grün. Der alte Nullable-Client-ID-Test erwartet daher keine erfolgreiche Kontofreigabe mehr für eine unbekannte bereits ausgeführte Order; stattdessen wird ausdrücklich der fachliche, nicht ein `localeCompare`-Fehler geprüft.
- `kraken_history.py` liest die dokumentierten v3-Envelopes für Executions, Orders und Triggers über CCXT. Der installierte SDK-Parameter `version=v3` wird durch einen echten lokalen Signing-Test geprüft; kein eigener HTTP-/Signaturcode und keine globale SDK-Optionsänderung. Leere Zwischenantworten, Header-/Body-Cursor, Überlappung, 1.205 Ausführungen/Orderereignisse und beide Seiten eines Trigger-Edits sind geprüft. Provider-Account-UID bleibt über Seiten, Quellen und SQLite-Fortsetzung gebunden; Widersprüche stoppen den Fortschritt.
- Kraken-Historienorders sind Ereignisbelege, keine erfundenen aktuellen offenen Orders. Alle alten/neuen Edit-Seiten bleiben erhalten. `trading_history_evidence.ts` klassifiziert eigene historische Ereignisse nur bei exakter Konto-/Exchange-ID-/Symbol-/Seiten-/Reduce-only-Zuordnung und belegter kumulativer Fillmenge; es verändert keine aktuelle Order. Tatsächliche Kraken-History-Gebühren werden nicht aus SDK-Raten geschätzt, fehlende Beträge bleiben ungeklärt; Gebührenwährung bleibt unbekannt, wenn der Endpoint sie nicht belegt. Das vollständige Gebühren-Ledger aus 005 bleibt offen.

## Nachgewiesene Fortschritte in 003 und Recovery-Anschluss

- Migration 30 ergänzt dauerhafte Entry-Abbruchwünsche, Grund und letzte Bearbeitung direkt an den vorhandenen Orders; kein Datenverlust und keine Rücksetzung. `trading_entry_commitment.ts` behandelt alle nichtterminalen Zustände und ungeklärte Journaloperationen als Verpflichtungen, unabhängig von lokaler Positionsmenge.
- Globaler und Konto-Kill-Switch schreiben zuerst die Sperre und fordern anschließend den Entry-Abbau an. Die Konten werden trotz einzelner Fehler weiter abgearbeitet. Pro Konto/Durchlauf maximal fünf Verpflichtungen; zehn Sekunden Abstand zwischen Bearbeitungen. Auch unlösbare alte Verpflichtungen rotieren, damit andere stornierbare Orders nicht verhungern. TTL-Abbrüche verwenden denselben dauerhaften Weg.
- Nichtterminaler Cancel, Timeout und fehlende gebundene Identität führen zu einer weiterhin sichtbaren Kontosperre, nicht zu einem erfolgreichen Abbau. Wiederholter Klick und Neustart wiederholen keinen möglicherweise gesendeten Cancel blind. Ein neuer Versuch derselben exakten Order benötigt einen nach dem vorherigen Versuch begonnenen aktuellen Orderbeleg; der alte Versuch wird ausdrücklich als „weiter aktiv“, nicht als „Order storniert“, protokolliert.
- Die periodische Reconciliation setzt gespeicherte Abbrüche fort und liest auch nach fehlgeschlagenem Cancel neue Fills/Schutzdaten. Sicher nie gesendete vollständige Pläne werden lokal und transaktional beendet. Der Operator-Flatten-Einstieg fordert den Entry-Abbau auch bei `quantity=0` an.
- `trading_lifecycle.ts` prüft vor Abschluss terminale Entries, Exit-Siblings, ungeklärte Operationen und exakte eigene Nullbilanz. Ein gefüllter Stop bei weiterhin aktivem Entry kann keinen Trade mehr schließen und fordert dessen Abbau an. Lokale Nullpositionen mit bereits vollständig belegtem Entry-/Exit-Roundtrip schließen nun korrekt; Entry-Durchschnitt wird aus realen eigenen Fills rekonstruiert.
- Noch offene eigene SL-/TP-Siblings werden vor Abschluss journalisiert storniert; ausschließlich nachweislich ungesendete Exits werden lokal beendet. Nach Cleanup/Flatten/Stop-Änderung wird frisch abgeglichen; insgesamt höchstens drei Snapshots je Durchlauf. Verbleibende Ungewissheit setzt sich im nächsten Durchlauf fort. Positionsabschluss und zulässiger Intent-Übergang sind transaktional; ein bereits terminaler Intent wird nicht wiederbelebt.
- Neue Regressionen waren zuerst rot: offener Cancel als scheinbar fertiger Aufruf, Abschluss trotz weiter teilgefülltem Entry, fehlender Roundtrip-Abschluss ohne Zwischenposition. Zusätzlich getestet: Kill-Abbau bei Nullposition, unbekannte Identität, Wiederholung/Neustart, frische Cancel-Retry-Evidenz, Kontounabhängigkeit, begrenzte faire Bearbeitung und Exit-Sibling mit unklarem Cancel. Ein separater Kindprozess wird wirklich während `dispatching` hart beendet; das Journal und `cancel_pending` bleiben erhalten und verhindern einen Blind-Retry. Weitere harte Abbruchphasen bleiben offen.
- Migration 31 speichert Emergency-Wunsch und Ursache auf der aktiven Position; bereits bestehende Emergency-Positionen werden ohne Reset übernommen. Globaler Operator-Flatten speichert zunächst alle ausgewählten Kontoanforderungen und beginnt erst danach Providerarbeit. Ein fehlerhaftes Konto verhindert nicht die Reduktion eines separat belegten anderen Kontos. Zwei-Phasen-Reihenfolge ist mit zwei Konten und Prüfung vor jedem Fake-Provideraufruf nachgewiesen; kein globales transaktionales Emergency-Request-Objekt behauptet.
- `trading_emergency.ts`: interner Emergency-Pfad liest unabhängig von lokaler Nullmenge frische Orders/Fills/Position. Ungeklärter Entry-Cancel überspringt die Reduktion belegter eigener Menge nicht mehr. Jeder Late-Fill verlangt nach geklärtem vorherigem Flatten eine neue persistierte Generation. Unklare Flatten-Ergebnisse werden nicht blind wiederholt; reine `filled`-Flags ohne vollständige ausgeführte Menge genügen nicht. Bei fremder Same-side-Differenz bleiben Reduktion und bestehende Schutzorders unangetastet. Wiederholung/DB-Neuöffnung, versteckte verlorene Bestätigung und Hyperliquid-/Bybit-/Kraken-Profil-Fakes bestehen.
- Harte Kindprozessabbrüche unmittelbar vor Dispatch eines vorbereiteten Flatten und nach realem Paper-Accept vor Engine-Bestätigung bestehen. Nach Neustart entsteht genau eine wirtschaftliche Paper-Ausführung mit derselben lokalen Order-ID. Positive No-Send-Evidenz erlaubt ausschließlich vorbereitete/aufgegebene eigene Operationen mit unveränderter Kontoidentität, Credential-Generation und identischem Request. `dispatching` wird dadurch nicht zurückgesetzt.
- `trading_protection.ts`: gemeinsamer Stop-Prüfer für Engine und lokale Monitoring-Projektion. `open` und `partially_filled` benötigen eine positive Restmenge, ausreichende Deckung, gebundene Konto-/Trade-/Orderidentität, Symbol, Gegenseite, Reduce-only und sicheren Trigger. Schutzbedarf ist aktuelle eigene Menge plus noch mögliche Entry-Reste, nicht nochmals die ursprüngliche Bruttomenge. Fehlende ausgeführte Menge bleibt unbekannt. Pure Negativtabellen prüfen alle Status-/Identitäts-/Seiten-/Mengen-/Triggerfälle; Monitoring-Regressionen nutzen dieselbe Prüfung. Das ist ausdrücklich noch kein vollständiger frischer Account-SafetyProof.
- Neue echte Paper-Regression zunächst rot: ein schon teilweise ausgeführter ausreichender Stop löste einen unnötigen Ersatz und eine falsche Emergency aus. Jetzt bleibt dessen ID unverändert. Ein tatsächlich teilgefüllter Ersatzstop führt vor Geschwister-/TP-Änderungen zu frischer Evidenz; Stale-Stop-Cancel ebenfalls. Bereits nachgezogene Stop-Preise bleiben auch bei fehlender alter Stop-Order erhalten. Der alte `filled`-Stop-Kurzschluss wurde durch eine frische Ownership-/Schutzprüfung und Entry-Drain ersetzt. Die Engine korreliert nun auch Nicht-CCXT-Adapter über denselben exakten Order-/Fill-Prüfer; keine Preisheuristik.
- Paper-Teilfüllungsmodell: kumulative Menge, VWAP, reale einzelne Simulations-Fill-IDs, Restorder und deterministische Liquiditätsgrenze. Ein bereits ausgelöster teilgefüllter Market-Stop bleibt nach Preisrücklauf und Neustart ausführbar. Default: Reduce-only-Überhang bei Nullposition storniert; ausdrückliche Fixturevariante behält ihn. Zunächst rote Regression „Teilmenge fälschlich filled“ ist grün. Alle Simulationslimits sind optional, keine neue produktive Kontoeinstellung oder Börsenzertifizierung.
- Migration 32 und `trading_take_profit.ts` speichern die TP-Zielbudgets, bereits beobachteten Ziel-Fills und echte Abschlussbelege samt Plan-Hash und Versionsprüfung. Verteilung basiert auf belegter eigener Restmenge und noch nicht verbrauchten Zielbudgets. Vollständig erledigte gespeicherte Ziele bekommen keine neue Zuteilung; eine bloße Reduzierung/Rundung auf null zählt nicht als erreichter TP für adaptive Stops. Nicht handelbare Rundungsreste werden als `unallocated_quantity` behalten, nicht in Orders erfunden. Keine rückwirkende Legacy-Zertifizierung aus diesem neuen Datensatz ableiten.
- Reproduzierte Regression: nach 0,300 Stop-Ausführung wurden auf nur 0,027 Restposition nochmals TPs für die volle Entry-Menge 0,327 angelegt. Jetzt bleibt die Summe bei 0,027, Preise unverändert. Neustart behält Budgets und IDs; ein danach wirklich erledigter kleinerer TP wird nicht wieder auf die Brutto-Entry-Verteilung aufgefüllt. Reine Tests decken Rundung ohne Budgeterhöhung, Cancel-Race-Überfüllung, Staub, gespeicherten Abschluss und Erschöpfung der Ziele ab.
- Initiale TPs nutzen jetzt einen echten eigenen Fill-/Positionsabgleich, nicht ausschließlich den Entry-Ack. TP-Cancels werden vor Ersatz in einem separaten Durchlauf abgeschlossen; höchstens fünf pro Snapshot-Pass. Ein neuer Snapshot muss danach aktuelle Orders/Fills/Ownership beweisen. Auch nach einer TP-Submission wird frisch gelesen; ein auf Annahme bereits gefüllter/teilgefüllter TP stoppt weitere Submits dieses Durchlaufs. Fehlende Cancel-Ausführungsmenge bleibt im Operationsjournal ungeklärt und verhindert Ersatz. Neue Generations-Requests werden nicht über versandte historische Requests geschrieben.
- `recoverPreparedExits` teilt den strengen lokalen No-Send-Beweis nun zwischen Stop, TP und Flatten: passende Identität/Credential-Generation, Request-Prüfsumme, exakt ein eigenes erwartetes Exit, unveränderte gespeicherte Orderfelder, keine Fills und ausschließlich vorbereitete/aufgegebene Submit-Operationen. Harte TP-Prozessabbrüche bei `created`, nach lokalem `submitting` vor Dispatch und nach Paper-Accept bestehen. In allen Fällen genau eine Paper-Order mit identischer dauerhafter ID. Negativtests verweigern Reset bei anderer Identität, Dispatch-Phase, falschem Request-Hash, leerem Erwartungsset oder geänderter Menge.
- Eine weitere zunächst rote Regression ist repariert: Ein korrekt vorankommender Abgleich, der sein Budget von drei Snapshots erreicht, ist jetzt eine vorübergehend ausstehende Fortsetzung, kein falscher harter Kill-Switch. Neue Entries bleiben über den offenen Reconciliation-Incident gesperrt, bis ein vollständiger Durchlauf gelingt. Echte Vertrags-/Cancel-/Zuordnungsfehler bleiben hart. Unaufgelöste TP-Metadaten dürfen zudem die unabhängige Wiederherstellung eines frisch belegten eigenen Stops nicht verhindern: keine unbewiesene zusätzliche Stop-Anhebung, bisher sicherster Trigger bleibt erhalten, TP-Fehler und Kontosperre bleiben sichtbar.

## Tatsächlich ausgeführte Prüfungen

### Weitere lokale Fortschritte: explizite Freigabe und Legacy-TPs

- `trading_safety_proof.ts` liefert strukturierte Ergebnisse samt stabilen Reason-Codes, Konto-/Credential-/Versions-/Runtime-Bindung, Evidenz-Hash und Aufnahmezeit für die fünf geplanten Zwecke. Reine Tabellen prüfen terminale und unsichere Entries/Exits, Nullposition mit weiter aktivem Entry, partielle Stops, ungeschützte/fremde/unbewiesene Mengen, historische terminale Orders und ungeklärte Operationen.
- Die Account-Kill-Switch-Freigabe ist über `trading_safety_repository.ts` angeschlossen: zwei erzwungene Abgleiche plus neuer Kontostand, dann gemeinsame Proof-/Schreibtransaktion unter Coordinator. Fehlende/unknown/partial Quellen, zu spätes Fill-Startfenster, veraltete Aufnahme, ungültiger Kontostand, veränderte Kontoversion, unerledigte Operation oder neue Operator-Sperre verhindern das Entsperren. Der letzte Fence nach dem lokalen Update kann die ganze Freigabe zurückrollen. Eine belegte bestehende Paper-Position bleibt dabei unverändert geschützt; keine neue Order/kein Flatten durch Freigabe.
- Zuerst rot nachgewiesen: zwei erfolgreiche leere Abgleiche ohne Acquisition-Beleg ließen die Freigabe durch. Jetzt `ACQUISITION_MISSING`, Kill bleibt aktiv. Weitere echte Kontotests prüfen partielle/unbekannte Quellen, fehlende Baseline-Abdeckung, Staleness, Balancefehler, Zustandsrennen und noch vorbereitete Operationen. Paper-Acquisition ist eine vollständige gemeinsame Datenbanktransaktion; keine Atomizitätsbehauptung für externe REST-Endpunkte.
- `trading_take_profit_recovery.ts` ersetzt pauschale Originalplan-Rekonstruktion bei fehlenden Metadaten durch pristine No-Send-Initialisierung oder belegte aktuelle/abschließend ausgeführte Zielgenerationen. Zuerst rot: Wiederaufnahme nach Verlust der Metadaten handelte ein schon erledigtes kleineres Ziel erneut und änderte den verbleibenden TP. Jetzt bleiben aktive ID, Restmenge und Fill-Anzahl unverändert. Provenienz wird transaktional als `TP_ALLOCATION_RECOVERED` gespeichert und bei normalem weiteren Abgleich nicht dupliziert.
- Mehrdeutige Legacy-Cancel-Lücken/Requests/Pending-Zustände bleiben `TP_ALLOCATION_REVIEW_REQUIRED`, ohne neue TP-Order oder künstliche Quote. Regression: fehlender SL wird unabhängig wiederhergestellt, während die Kontosperre und Review-Anforderung über Neustart erhalten bleiben. Vollständige Legacy-Restore-/Pending-Generation-Abdeckung ist damit noch nicht abgeschlossen.
- Grenze: Die reinen Proof-Zwecke Entry-Admission/Drain/Closure/PositionProtected sind noch nicht in allen vorhandenen Engine-, Global-Release- und Monitoring-Pfaden verkabelt. Externe Baseline-Klassifikation, vollständige aktuelle Provider-Quellen und Retentionsbeweise bleiben offen; das neue Konto-Freigabegate ist kein Nachweis abgeschlossener Pakete 001–003.

Runtime: Node **22.23.2**, Python **3.12.13**, CCXT **4.5.75**. Ausschließlich lokale HTTP-Fakes und temporäre Datenbanken; keine echten Exchange-Aufträge. Die zuvor im Temp-Verzeichnis verwendeten Node-/Python-Runtimes waren während dieses Durchlaufs nicht mehr vorhanden. Ersatz liegt isoliert unter `C:/Users/nikla/AppData/Local/tsx-core-dev-runtime`: `node-v22.23.2/node.exe` (gegen offizielles SHASUMS256-Manifest geprüft) und `ccxt-py312/Scripts/python.exe`. Python-Abhängigkeiten unverändert mit `uv pip --require-hashes` aus `exchange_executor/requirements.lock`; keine globale Node-/Python-Umschaltung. Der vorhandene eigenständige Ruff unter `C:/Users/nikla/AppData/Local/Temp/tsx-core-ccxt-py312/Scripts/ruff.exe` läuft weiterhin.

- Vor Reparatur rot: Node akzeptierte eine Antwort für die falsche Request-ID; Python erfand IDs/akzeptierte abweichende IDs und vertauschte Batch-Legs. Nach Reparatur grün.
- Gezielt grün: `test_trading_state_transitions`, `test_trading_mutation_coordinator`, `test_exchange_contract_validation`, `test_trading_order_repository`, `test_trading_engine`, `test_trading_failures`, `test_ccxt_exchange`, `test_trading_web_control`, `test_signal_schema_migration`, `test_configurable_fallback_migration`, `test_dynamic_exchange_registry`.
- Aktueller Python-Gesamtlauf: **104 Tests grün**, einschließlich aktueller Provider-Scope-/Pagination-/Zeitprüfungen, echter CCXT-Parser mit Netzsperre, Kraken-History und Cancel-Fällen. Fehlende historische Mengen werden nicht zu Null. Ruff ebenfalls grün.
- TypeScript-Prüfung grün; ESLint/Ruff ohne Fehler. Architektur-Gate grün. Nach kleiner Repository-Extraktion Komplexitätsgate wieder grün: **0 Warnungen**, schlechtester Zykluswert 15; keine Budgets gelockert.
- Aktueller JavaScript-Gesamtlauf: **90 Testdateien grün**, einschließlich strukturierter SafetyProof-/Kontofreigabetabellen, finalem Fence-Rollback, Beibehaltung einer geschützten Position, fehlender/veralteter/unvollständiger Quellen, Legacy-TP-Wiederherstellung/Review, TP-Restverteilung, Cancel-/Fill-Races, TP-Hard-Crash-/Negativbeweisen, unabhängiger SL-Verwaltung bei defekten TP-Metadaten, Emergency-Hard-Crash und Multiaccount-Reihenfolge, Paper-Teilfüllungen, gemeinsamem Stop-Schutz, Entry-Drain/Lifecycle, dauerhafter History-Checkpoints, Recovery-, Frontend-, Backup-, MCP-, Architektur-/Governance-Gates. Typecheck und ESLint ebenfalls grün; Ruff grün. Architektur: **94 Module, 330 interne Imports, 0 Zyklen**; Komplexität: **0 Warnungen**, Maximalwert **15**, längste Funktion **96 Zeilen**; keine Budgets gelockert. Migrationen bis Version **32** getestet; TP-Allokationstabelle gehört zum geforderten Backup-Schema. Verkürzte Alt-Schema-Fixtures enthalten die tatsächlich vorausgesetzten historischen Tabellen/Spalten, damit die Migrationen gegen gültige Altschemata geprüft werden; Migrationen selbst wurden dafür nicht abgeschwächt. Der bestehende Cancel-Timeout-Test aktiviert seinen Fehler jetzt nach dem initialen erfolgreichen Abgleich, damit er weiterhin gezielt den späteren Stop-Ersatz testet. Das ist kein aktueller SonarCloud-/Provider-/Release-Nachweis.
- Neue Fälle zuerst rot nachgewiesen: Deaktivierte Route während Vorbereitung ließ Orders durch; unbekannte Client-ID-Fills verschwanden; fehlende Provider-IDs wurden erfunden/verworfen; Fremdmenge löste Schutz-/Flatten-Mutationen aus. Diese Regressionen sind jetzt grün.
- Ebenfalls zuerst rot, jetzt grün: unveränderter vorbereiteter Plan wurde durch einen erneuerten Erstellungszeitpunkt abgelehnt; abgelaufener ungesendeter Plan ließ eine `opening`-Reservierung zurück. Neustart-, Ablauf-, Deaktivierungs- und Planänderungsfälle geprüft.

## Weitere Fortschritte: aktuelle Provider-Bereiche und Positionsnamespace

- `current_state.py` ist in den tatsächlichen Open-State-Pfad integriert. Bybit liest alle Cursorseiten und linearen Settlement-Bereiche, einschließlich USDC; zusätzliche nicht verwaltbare Produktbereiche werden explizit erkannt. Kraken-Listen werden accountweit, Hyperliquid-Listen nach DEX-Discovery je Bereich eingelesen. CCXT-Collection-Helper können keine weiteren Seiten oder fehlende Pflichtsammlungen mehr unbemerkt ausblenden. Historische Ergänzungen behalten einen separaten, nicht vollständigen Fill-/Retention-Nachweis.
- Pflichtsammlungen, eindeutige Cursor, Bereichsbindung und Providerzeit werden vor Normalisierung geprüft. Fehlende Seiten/Budget/Deadline/veraltete Providerantwort: strukturierte `CURRENT_STATE_INCOMPLETE`/503, keine abgeschnittene Erfolgsantwort. Harte Vertrags-/Scopefehler bleiben getrennt. Neue aktuelle Abfragen sind auf 64 SDK-Aufrufe und die absolute Deadline mit Antwortreserve begrenzt; bestehende zusätzliche History-/Recovery-Arbeit weiterhin gemeinsam fünf. Bereichs-/Seitenbelege werden validiert, redigiert und dauerhaft gespeichert, nicht als atomarer oder historischer Komplettbeweis interpretiert.
- Zunächst rote Adapterregression: der alte Listenpfad ließ die mehrseitigen Konto-Fakes aus. Jetzt 506 Bybit-Orders/206 Positionen vollständig, einschließlich leerer Zwischenseiten und Überlappung. Echter CCXT-Parser-Test hält 605 Kraken-Orders sowie konditionale Hyperliquid-/Bybit-Stops mit fehlenden Client-IDs und kumulativen Restmengen. Fehlende Collection, falscher DEX/Settlement, wiederholter Cursor und veraltete Providerzeit bleiben sichtbar. Bewegte Markpreise/PnLs allein machen identische überlappende Positionsmengen nicht ungültig; veränderte Exposure tut es.
- Zunächst rot: gleicher Coin/Seite/Menge in einem anderen Provider-Markt wurde als eigene Position akzeptiert. Positionen tragen jetzt `providerSymbol`; Engine-Ownership und Konto-Freigabekollektor prüfen den exakten Markt aller eigenen ausgeführten Entries. Die Regression belegt Kontosperre ohne Ordermutation. Eine bestehende Partial-Fill-Fixture enthält jetzt den tatsächlich zugehörigen Markt; unveränderte Mengen-/Stop-/TP-Erwartungen.
- Vollständige aktuelle Listen ersetzen weiterhin weder eine externe historische Konto-Baseline noch die offenen SafetyProof-/Recovery-/Providerabnahmen. Keine Änderung an Handelsparametern, kein neuer ausführbarer Produkttyp, keine Serververbindung und kein Git-Publish.
- Erneut vollständig ausgeführt: **90 JavaScript-Testdateien und 104 Python-Tests grün**, Typecheck, ESLint, Ruff, Architektur (94 Module/330 interne Imports/0 Zyklen), Komplexität (0 Warnungen/Verletzungen, Maximalwert 15, längste Funktion 96 Zeilen) und Diff-Whitespace-Prüfung grün. Scope-Persistenz über DB-Neustart, lokale HTTP-503-Wiederholung ohne Orderwrite, andere Settlement-Position und unveränderte geschützte Kontofreigabe sind in diesen Tests enthalten. Keine SonarCloud-/Providerkonto-/Release-Abnahme.

## Weitere Fortschritte: nachgewiesene flache Konto-Baseline

- Migration 33 ergänzt `trading_account_baselines` sowie Identitäts-/Baseline-/Reviewfelder an der Remote-Evidenz. Altdaten bekommen **keine geratenen** Kontofingerprints. Neue Belege verwenden eine identitätsgebundene v2-Deduplizierung; ungebundene alte Belege bleiben ungeklärt. Die Baseline gehört zum geforderten Backup-Schema; bestehende Migrationen/Daten wurden nicht zurückgesetzt.
- `trading_account_baseline.ts` und `trading_baseline_classification.ts` sind im echten Reconcile-Pfad angeschlossen. Zwei frische, nicht überlappende, vollständig scoped aktuelle Orders-/Positionsbeobachtungen müssen flach sein; zusätzlich müssen alle lokalen Orders terminal, die eigenen Fill-/Orderbilanzen je Intent null und alle Operationen/Positionen beendet sein. Falsch terminale Mengen, fehlende Fills und unterschiedliche Markt-Namespaces dürfen keine Baseline erzeugen. Belegfenster, Konto-/Credential-/State-Version, Ledger-Hash und Counts werden gespeichert; `ACCOUNT_BASELINE_ESTABLISHED` erscheint einmalig im Journal.
- Nur eindeutig identifizierte, kontogebundene und **vor** der unveränderlichen Grenze liegende unbekannte Fremdereignisse werden als `external` klassifiziert. Keine eigenen Fills/PnLs werden daraus gebucht, keine Order wird verändert, keine Sperre wird aufgehoben. Fehlende IDs/Zeitpunkte, Legacy-Belege ohne Fingerprint, Konflikte und Ereignisse ab der Grenze bleiben ungeklärt. Wiederholte flache Snapshots schieben die etablierte Grenze nicht weiter. Pro Durchlauf maximal 500 faire Review-Kandidaten; später entdeckte lokale Referenzen widerrufen die externe Klassifikation und verlangen erneut exakte Korrelation.
- Recovery und Release verwenden dasselbe erforderliche Startfenster. Nur die belegte Baseline erlaubt eine Vorwärtskorrektur eines alten pauschalen History-Startpunkts; jede ältere lokale Order erweitert das Fenster wieder rückwärts. Checkpoints werden dabei revisionsgebunden zurückgesetzt, alte In-flight-Antworten abgewiesen. Historische eigene Orders werden durch die Baseline nicht still aus dem benötigten Fenster entfernt.
- Zunächst rote echte Engine-Regressionsfälle: ein nachweislich altes fremdes Fill blieb auch nach den vollständigen flachen Folgeabfragen ungeklärt; der History-Start blieb trotz belegter Grenze beim pauschalen Konto-Erstellungszeitpunkt. Jetzt repariert. Zusätzliche Tests prüfen Replay, Restart, einmaliges Journal, unveränderten Kill-Switch, fehlende/partielle/stale Quellen, falsche Identität, fremde Exposure, Credential-Wechsel zwischen Beobachtungen, DB-Rollback, post-Baseline-Ereignisse, Konflikte, Altbelege und lokale Ledger-Negativfälle.
- Orderbelege übertragen optional die echte letzte Provider-Ereigniszeit. Fehlende Updatezeit bleibt null; weder Erstellungszeit noch lokale Empfangszeit wird als terminale Ereigniszeit erfunden. Echter Bybit-/CCXT-Parser-Test deckt den Transport ab.
- **Weiterhin offen:** Der Baseline-Nachweis ist keine vollständige laufende Fill-Historie und keine universelle REST-Atomizität. Die noch pauschal `unknown` ausgegebene Fills-Quelle muss mit nachgewiesener fortgesetzter Coverage/Retention verbunden werden; neue Entry-, Closure-, Global-Release- und Monitoring-Verbraucher des SafetyProof bleiben ebenso offen wie die übrigen Teile von 001–008. Keine Provider- oder Produktionsfreigabe.

## Weitere Fortschritte: fortgesetzte Fill-Coverage und unabhängiger Review

- `history_coverage.py` und `exchange_history_coverage.ts` trennen einen belegten, zusammenhängenden Ausführungszeitraum vom bloßen `scannedThrough`-Cursor. Der Nachweis ist profilgebunden; spätere Fenster schließen keine ältere Lücke. Node prüft Rückschritte, Profilwechsel, fehlende Seiten und unmögliche Sprünge; Speicherung folgt weiterhin der Aufnahme der Ereignisse mit Revision/CAS und gemeinsamem Rollback. Alte Checkpoints ohne Coverage werden einmalig auf ihren ursprünglichen benötigten Anfang zurückgesetzt. Eine neue ältere Verpflichtung oder belegte Baseline-Korrektur invalidiert auch Coverage ausdrücklich.
- Der tatsächliche Python-Open-State-Pfad und Node-Executorclient verwenden den Nachweis. Nur ein neu gelesener, vollständiger und mindestens bis zum aktuellen Acquisition-Start reichender Zeitraum darf die Fills-Quelle bestätigen. Kraken-v3 verlangt zusätzlich seine authentifizierte Account-UID; fehlende UID bleibt auch auf der Node-Seite ein Vertragsfehler. Konto-Freigabe prüft denselben Nachweis, nicht nur das vom Executor behauptete `complete`. Ungeklärte Ereignisse und Ownership-Konflikte bleiben unabhängig blockierend.
- Hyperliquid prüft nach dem zeitgefilterten Abruf die ungefilterte Retention. Ein vorhandener Zeitzeugen-Fill muss **strikt älter** als der benötigte Fensteranfang sein: Bei gleichem Zeitstempel könnte das 10.000-Fill-Limit einen Teil dieser Millisekunde abschneiden. Permanente Regression mit 10.001 ursprünglichen/10.000 verfügbaren Fills sowie unabhängiger read-only Gegenprüfung bestätigt `unknown` statt einer falschen Freigabe. Jeder Retention-Aufruf zählt im gemeinsamen Fünf-Aufruf-Budget; fehlendes Budget quittiert kein Fenster.
- **Offener Hyperliquid-Livenessfall:** Ein zuvor vollständig leeres Konto mit seinem allerersten neueren Fill benötigt ergänzend einen vollständigen, restartfähigen Total-Retention-Nachweis unter 10.000 Fills. Der einfache ältere-Zeitzeugen-Pfad kann diesen Fall noch nicht positiv beweisen. Bis zur geprüften Ergänzung bleibt er absichtlich ungeklärt; keine fertige Hyperliquid-Abnahme behaupten.
- **Bybit bleibt accountweit unbewiesen:** Die geprüfte Execution-API verwendet bei Optionen ohne `baseCoin` BTC als Default; aktuelle Basiswertlisten beweisen keine delistete Historie. Außerdem existiert getrennte Pre-upgrade-Historie. Der zunächst zu breite Coverage-Ansatz wurde durch den unabhängigen Review korrigiert: `bybit_v5_linear_endpoint_v1` bezeichnet ausschließlich diesen Endpunkt, niemals vollständige Konto-/Upgrade-Historie. Die Fills-Quelle bleibt `unknown`/`option_history_scope_unproved`. Zusätzliche inverse/spot/option-Negativprüfungen erkennen fremde Aktivität, heben diese Grenze aber nicht auf. `execType=Trade` wurde entfernt, damit ADL-/Settlement-Ereignisse nicht ausgefiltert werden.
- Neue Tests zuerst rot: fehlender persistierter Zeitraum, nicht geprüfte Fremdkategorien, übersprungenes Retention-Budget und von Node still verworfene Coverage-Felder. Aktuell **115 Python-Tests grün**, Ruff grün; die fünf gezielten Node-Dateien `test_trading_history`, `test_exchange_history_coverage`, `test_ccxt_exchange`, `test_trading_account_baseline`, `test_trading_safety_proof` grün. Echte lokale HTTP-Clientintegration und Python-Open-State-Integration sowie DB-Neustart, Legacy-Reset und Rollback sind enthalten. Typecheck, Architektur (99 Module/356 Imports/0 Zyklen) und Komplexität (0 Warnungen/Verletzungen, Maximum 15) im gemeinsamen Zwischenstand grün. Keine Budgetlockerung.
- Der vor den parallelen 006-Änderungen nachgeholte Gesamtlauf bestand **91 JavaScript-Testdateien**. Ein neuer Gesamtlauf mit Migration 34 stoppte an der verkürzten v24-Fixture von `test_trading_order_migration` (fehlende historische `signals`-Tabelle); diese Integrationskorrektur liegt bei Paket 006. Den Gesamtstand daher noch nicht als erneut vollständig grün ausgeben.
- Auf ausdrücklichen Nutzerwunsch arbeiten jetzt getrennte Subagents an 006, 008 und Sicherheitsreview. 006/008 sind **IN PROGRESS**, keine abgeschlossenen Pakete. Hauptagent integriert und prüft gemeinsame Schnittstellen; kein Commit/Push, keine Server-/Kontoabfrage und kein Deployment.

## Neuere Teilübergaben (2. September, nach obigem Zwischenstand)

- 006 ist lokal implementiert und gezielt geprüft: [006-IMPLEMENTATION-NOTES.md](006-IMPLEMENTATION-NOTES.md). Die anschließend bestätigte integrierte Baseline bestand 100 Node-Testdateien unter Node 22; spätere 004-/005-/007-Änderungen sind damit nicht automatisch vollständig abgenommen.
- Hyperliquid Total-Retention-Probe ist lokal ergänzt und fokussiert getestet; der oben noch offene Erstfill-Livenessfall wurde damit bearbeitet: [002-RETENTION-NOTES.md](002-RETENTION-NOTES.md). Echte Providerabnahme und übrige Historien-/Ownershippflichten bleiben offen.
- 004 gemeinsame absolute Entry-TTL lokal implementiert: [004-ENTRY-TTL-IMPLEMENTATION-NOTES.md](004-ENTRY-TTL-IMPLEMENTATION-NOTES.md). Tatsächlicher Modusreadback ist aktuell in Arbeit; Preisdeckel und vollständige notionalabhängige Tiers noch nicht abgeschlossen.
- 005 besitzt Migration 35, immutable Geldereignisse/Bewertungen, Rebate-Korrektur, Funding-/Reportingvertrag und enges Entry-Accounting-Gate: [005-IMPLEMENTATION-NOTES.md](005-IMPLEMENTATION-NOTES.md). Durchgängige Fill-/Daily-PnL-/Analytics-Projektion folgt gerade; keine Gesamtfertigstellung behauptet.
- 007 Ownership-Handoff und neue produktive Startup-/DB-Lifecycle-Integration: [007-OWNERSHIP-NOTES.md](007-OWNERSHIP-NOTES.md), [007-STARTUP-INTEGRATION-NOTES.md](007-STARTUP-INTEGRATION-NOTES.md). Zehn fokussierte Node-Dateien plus gesonderter echter SQLite-Close/Ack-Test grün; Wartungs- und Backup-Integration bleiben in Arbeit.
- 008 hat lokale Quality-/Acceptance-Werkzeuge: [008-IMPLEMENTATION-NOTES.md](008-IMPLEMENTATION-NOTES.md). Keine reale Provider-/Releaseabnahme.
- Neuer ausdrücklicher Zusatzauftrag [009](009-additional-ccxt-derivatives.md) wird erst nach Reparaturen 001–007 und vor der finalen Gesamtprüfung ausgeführt: sämtliche leicht integrierbaren CCXT-Futures-/Perp-Börsen mit gleichwertiger TSX-Prüftiefe. Bisher nur eingeplant.

## Noch offen – keinesfalls als DONE ausgeben

001/002: vollständiger Start-/Recovery-Worker auf Basis des Journals, harte Prozessabbrüche an allen verbleibenden Phasen und Legacy-Restore-Fällen; externe historische Quellenabdeckung, Einbindung persistierter Cursor/Completeness/Baseline in den durchgängigen SafetyProof; expliziter Parent-/Batch-Beweis für bislang ungebundene Attached Stops; sicher klassifizierte externe Historie/Baseline und belegter Umgang mit tatsächlichen Provider-Retentionslücken. Aktuelle Order-/Positionsbereiche sind inzwischen wie oben beschrieben angebunden; das ersetzt weder historische Fill-Beweise noch Providerkontoabnahme. Kraken-Client-only-Recovery bei verlorener Exchange-ID bleibt offen, obwohl die v3-Historienquellen jetzt angebunden sind. Fill-IDs sind im bestehenden Ledger noch nur kontoweit eindeutig; Namespace-Kollisionen werden derzeit als Konflikt festgehalten. Fehlende Gebührenangaben/mehrere Gebührenwährungen sind im Geld-Ledger-Paket zu bearbeiten.

003: gemeinsamer SafetyProof ist definiert und an Account-Release angeschlossen; vollständige Einbindung in Admission, Drain, Stopabdeckung, Closure und Global-Freigabe bleibt offen. Der belegbasierte Legacy-TP-Recovery-/Review-Pfad deckt pristine Originalpläne sowie eindeutige aktive/abgeschlossene Zielgenerationen ab. Weitere Legacy-Restore-/Pending-Generationsfälle, Ersatzorder-/Crashphasen und gemeinsam nutzbarer Exit-Cancel-Retry-Beweis nach erneut aktiv beobachteter Order bleiben offen. Gesamte aktuelle Provider-Quellen-/Baseline-Evidenz ist durch die neue lokale Lifecycle-/Schutz-/TP-/Freigabeprüfung nicht ersetzt. 004–008 sind überwiegend offen: Constraints, Gebühren/Funding, Ingress/Workflow/AI-Abrechnung, Start-/Backup-Wartungskoordination und Provider-/Release-Abnahme.

Die statische Provider-Allowlist ist weiterhin keine echte Testnet-/Live-Zertifizierung. Kein Commit, Push, Release, Serverkontakt oder Deployment im Rahmen dieser lokalen Umsetzung bislang ausgeführt.

## Weiterer lokaler Integrationsstand – 2. September

- 003: echte gemeinsame Closure-/Drain-Verbraucher mit Account-/Epoch-/Frische-Commitfences und eigenen Regressionstests: [003-LIFECYCLE-SAFETY-NOTES.md](003-LIFECYCLE-SAFETY-NOTES.md). Die Bybit-Quellenlücke wirkt jetzt auch im produktiven Drainpfad; Web-Control-Gesamtintegration daher nicht pauschal grün. Admission-/Schutz-/Global-Freigabe-Verbraucher weiterhin offen.
- 004: Mode-Readback und harte Entry-Preisgrenzen sind lokal übergeben: [004-MODE-IMPLEMENTATION-NOTES.md](004-MODE-IMPLEMENTATION-NOTES.md), [004-PRICE-IMPLEMENTATION-NOTES.md](004-PRICE-IMPLEMENTATION-NOTES.md). Zusätzliche tatsächliche SDK-Fixtures beweisen das Unterbinden ungewollter Hyperliquid-Builder-/Referralwrites bei REST und Pro. Leverage-/Notional-Tiers werden gerade implementiert; Kraken-Protected-IOC und Providerabnahme bleiben separat unbewiesen.
- 005: Migration 36 und tatsächliche Ledger-Projektionen sind ergänzt; Migration 37 für dynamische Risikobelege samt Retention-Anbindung in aktueller Integration. Finale Dispatch-Quellenfences und aktuelle Paper-Mark-PnL werden geprüft. Siehe laufende [005-IMPLEMENTATION-NOTES.md](005-IMPLEMENTATION-NOTES.md), nicht frühere Zwischenstandsangaben als Fertigmeldung lesen.
- 007: echte Offline-/DB-Leases und kohärente Backupgeneration sind lokal implementiert, inklusive harter Prozessabbrüche: [007-MAINTENANCE-NOTES.md](007-MAINTENANCE-NOTES.md), [007-GENERATION-IMPLEMENTATION-NOTES.md](007-GENERATION-IMPLEMENTATION-NOTES.md). Getrennte Snapshot-/Offsite-/Eligibility-/Drill-Nachweise befinden sich in abschließender Teilintegration. Kein alter boolescher Bypass als vermeintlich fertige Autorisierung.
- 009 weiterhin vollständig ausstehend und vor 008 eingeplant. Der aktuelle gemeinsame Quellbaum ist **noch nicht durch einen vollständigen grünen Gesamtlauf** abgenommen; keine Release-/Serverfreigabe.

## Jüngster Nachlauf: Freigabe, historische Nullbilanz und laufende Folgestufen

- 003 Globalfreigabe ist jetzt lokal implementiert: exakte UI-/API-Bestätigung, gemeinsamer frischer Account-Proof für jeden relevanten Account, kanonische Konto-Coordinatoren, erneute Kontenscope-/Versions-/Zeit-/Sperrprüfung nach dem Globalwrite und transaktionaler positiver Journalnachweis. Individuelle Kontosperren bleiben stehen; Execution startet nicht. Alte abgeschlossene/positionslose Trades benötigen Entryhistorie und eine belegte eigene Nullbilanz. Die einzelne Kontofreigabe hat ebenfalls einen letzten Frische-/Versionscheck nach ihrem Write. Details und Grenzen in [003-LIFECYCLE-SAFETY-NOTES.md](003-LIFECYCLE-SAFETY-NOTES.md).
- Neue `test_trading_global_release.js` wurde zuerst rot gegen die bisherige Freigabe ausgeführt und anschließend grün. Sieben gezielte Backenddateien (Globalrelease, SafetyProof, ControlRaces, LifecycleSafety, EntryCommitment, Emergency, EntryExpiry) sowie MCP-Controlplane und Web-Sicherheit grün. Frontend-Operations 22 Tests samt exaktem Bestätigungsdialog grün, Backend-/Frontend-Typecheck, eigener ESLint, Diffcheck, Architektur und Complexity grün. Das ist kein vollständiger aktueller Gesamtlauf.
- 004/3 Leverage-/Notional-Tiers hat einen vollständigen lokalen Teilhandoff: [004-TIER-IMPLEMENTATION-NOTES.md](004-TIER-IMPLEMENTATION-NOTES.md). 14 neue Pythonfälle und 16 gezielte Node-Dateien; Quellen-/SDK-/Scopebindung, tatsächliche quantisierte Mengen und finale Fences. Der frühere komplette Pythonstand bestand 192 Fälle; der parallele neue Funding-TDD-Test war im letzten 193er-Lauf noch absichtlich rot. Kraken-Protected-IOC sowie echte Provider-/Releaseabnahme bleiben unabhängig offen.
- 005 dynamische Reservierungen/db37 sind übergeben. Der fortsetzbare gemeinsame Account-log-Producer und Funding-Projektor ist freigegeben und in Umsetzung, Migration38 reserviert. Finanzbelege werden ausdrücklich nach beobachtetem Quellenstand bewertet, nicht als unveränderliche Providerfinalität ausgegeben; fehlende Scope-/Währungs-/Bewertungsbelege bleiben ungeklärt. Keine zweite Funding-Historienschleife neben dem gemeinsamen Fünfer-Recoverybudget.
- 002 Bybit-Recherche ist dokumentiert: [002-BYBIT-UTA-RESEARCH.md](002-BYBIT-UTA-RESEARCH.md). Freigegeben sind der eigenständige Scope-/Korrelationsconsumer und zusätzliche echte post-UTA2-Baseline-/Modusbindungen; Migration39 hierfür mit38 zu koordinieren. Keine Umbenennung von Buchungen zu Fills, keine Ableitung kontoweiter Finalität aus EOF oder Antwortzeit; vollständige Bybit-Freigabe bleibt ein offener Quellen-/Consumervertrag.
- 003 gemeinsamer Exit-Cancel-Retry inklusive eines einzigen fünfteiligen Cancelbudgets pro tatsächlichem Konto-Mutationskontext ist jetzt als nächste Teilstufe in Umsetzung. Keine parallelen eigenen Budgettöpfe für Entry, TP, Stop und Closure, keine Stopentfernung ohne aktuellen gedeckten Ersatz. Admission-/Schutz-/Monitoring-Verbraucher und vollständige Crash-/Provider-Integration bleiben danach erforderlich.
- 007 getrennte Backupbelege/isolierter Restore-Drill sind übergeben: [007-BACKUP-EVIDENCE-NOTES.md](007-BACKUP-EVIDENCE-NOTES.md); sieben passende Backup-/Generation-/Crash-/Replication-Dateien im Root-Nachlauf grün. Das ist keine automatische reale RPO-/RTO-/Powerloss- oder vollständige Paketfreigabe.
- 009 unverändert nach Abschluss der Reparaturen 001–007 und vor 008-Gesamtprüfung. Keine Börsenerweiterung ausgelassen oder durch bloße Flags ersetzt. Gesamtes Ziel bleibt aktiv, nicht fertig; keine Commits, Pushes, Releases, Server- oder realen Kontoaktionen.

## Jüngster Nachlauf: Entry-Admission, Funding und gemeinsame Integration

- 003 Entry-Admission besitzt jetzt den gemeinsamen kontoweiten SafetyProof vor Planung und im finalen tatsächlichen Dispatch-Fence. Die enge No-Send-Ausnahme ist durch privaten lebenden Witness, Original-ACK-/Versionsprüfung, Fremdpositionsrecount und mitgehashten Kandidatenkontext abgesichert. Unabhängige rote Review-Repros sind korrigiert, direkte API-/Engine-/DB-Reopen-/Witness-Tests grün: [003-ENTRY-SAFETY-IMPLEMENTATION-NOTES.md](003-ENTRY-SAFETY-IMPLEMENTATION-NOTES.md), [003-ENTRY-NOSEND-REVIEW.md](003-ENTRY-NOSEND-REVIEW.md).
- 003 Exit-Cancel ist lokal übergeben, mit einem tatsächlich gemeinsamen Fünferbudget, belegt aktivem Retry und sicherer Stopersatzprüfung: [003-EXIT-CANCEL-IMPLEMENTATION-NOTES.md](003-EXIT-CANCEL-IMPLEMENTATION-NOTES.md). Gemeinsamer produktiver ProtectionProof samt exakt gleicher, frisch invalidierbarer Monitoringprojektion wird parallel fertiggestellt; noch keine Paketabnahme.
- 002 Mode-/UTA-Ursprungsbindung und separater Scopeconsumer sind mit Migration39 übergeben: [002-SCOPE-IMPLEMENTATION-NOTES.md](002-SCOPE-IMPLEMENTATION-NOTES.md). Sie ersetzen ausdrücklich keine kontoweite Bybit-Fillfinalität. Offene Parent-/Batchbeweise und namespace-sichere Fill-/Moneyidentität werden anhand realer CCXT-/Primärquellen geprüft; aktuell bestätigt ist eine Kollision gleicher Provider-Fill-IDs verschiedener Märkte, nicht durch Datenlöschung zu bereinigen.
- 005 gemeinsamer Accountlog-/Fundingproducer ist mit Migration38 lokal übergeben: [005-FUNDING-COVERAGE-NOTES.md](005-FUNDING-COVERAGE-NOTES.md). 26 fokussierte JS-Dateien, vollständige211-Python-Suite, eigene Linter und Architektur-/Komplexitätsgate laut abgeschlossenem Teilhandoff grün. Gemeinsamer Hauptagent-Gesamtlauf bleibt separat erforderlich. Echte Live-FX-Bewertung, Kraken-Reporting/Cashlegkorrelation und unbewiesene Pre-UTA-/Retentionbereiche bleiben offen.
- Der gemeinsame JS-Lauf passierte nun auch die korrigierten vollständigen Legacy-Migrationsfixtures, stoppte danach an WorkflowFallbacks früher nur lokal fingierter Kapazitätsposition. Diese Fixture nutzt jetzt echte geschützte Papertrades; korrupter Altplan bleibt fail-closed ohne Börsenfallback. Wiederherstellung des Originalplans und tatsächliche sichere Kontofreigabe sind geprüft. WorkflowFallback vollständig grün; erneuter Gesamt-JS-Lauf gestartet, noch nicht als vollständig grün bestätigt.
- Gesamtumfang und Reihenfolge001–007 →009 →008 bleiben unverändert. Die vorherige kurze Prozentangabe war eine grobe Fortschrittsschätzung, kein abgeschlossener Anforderungs- oder Releaseaudit. Ausschließlich lokale Arbeitskopie und temporäre Testdaten; kein Commit/Push/Server-/Providerkontakt.

## Bestätigter Gesamtlauf und anschließende Wiederanlaufkorrekturen

- Der anschließende gemeinsame Backendlauf unter Node 22.23.2 bestand **alle 146 registrierten Testdateien** (Schema39, einschließlich Protection-Receipt, echter Workflow-Fallback-Positionen und Account-Retirement). Frontend: **22 Dateien / 126 Tests**, TypeScript-Projektbuild und Vite-Produktionsbuild ebenfalls grün. Das ist ein konkreter Zwischenstand, keine Abnahme der danach weitergeänderten Arbeitskopie, kein Coverage-/Sonar-/Provider- oder Releasebeweis.
- Zwei zuvor rote Integrationsfehler sind korrigiert: Ein sauber stillgelegtes Konto mit erhaltener Historie blockiert nicht mehr den globalen Entryabbau; deaktivierte Konten mit eigener Exposure werden beim tatsächlichen Startup- und periodischen Schutzlauf nicht übersprungen. Die gemeinsame Zielauswahl berücksichtigt offene Verpflichtungen, ungeklärte Evidenz und kritische Risiken. Widersprüchliche stillgelegte Konten werden nicht unsichtbar gemacht. Historie und Kontoeinstellungen bleiben erhalten. Nachweise: `test_trading_account_retirement.js` und `test_trading_recovery_worker.js`.
- Produktiver Schutz und Monitoring verwenden jetzt denselben gespeicherten, versions-/quellengebundenen Proof, nicht getrennte heuristische Stopchecks: [003-PROTECTION-IMPLEMENTATION-NOTES.md](003-PROTECTION-IMPLEMENTATION-NOTES.md). Der grüne 146-Dateien-Lauf enthält diese Teilintegration.
- Neue Kraken-Requests und der No-Send-Admissioncheck verwenden die identischen ursprünglichen geschützten Requests einschließlich belegter eigener Batchtags; vorhandene taglose Originalrequests werden nicht umgeschrieben. Tagged-/Legacy-Fälle samt echter DB-Neuöffnung sind grün. Das übrige Order-/Fill-Namespace-Paket samt Migration40 ist noch in Arbeit: [002-order-fill-identity-followup.md](002-order-fill-identity-followup.md).
- Der Journalwriter bindet jetzt auch den vollständigen unveränderlichen Outboundrequest, Konto-/Credentialidentität und erwartete Leg-IDs vor der Vorbereitung. Manipulation durch einen späteren Callback verhindert den Send, ohne Originaljournalbytes zu überschreiben. Drei neue Dispatch-Fence-Regressionen zeigten den Fehler zunächst rot; anschließend waren DispatchFence, EntrySafety, Recovery, EntryExpiry und Failure gemeinsam grün.
- Die neue echte Hard-Crash-Datei besteht neun Prozessabbruchstellen vom Plancommit bis zum Börsenresultat und abgeschlossenen Paper-Roundtrip, mit separatem tatsächlichem Adapteraufrufprotokoll. Ein weiterer roter Fall führte zur engen Reparatur des TTL-Drainpfads: Auch `submitting` mit positivem Never-Sent-Nachweis darf einen abgelaufenen ursprünglichen Plan lokal schließen; Unsicherheit allein berechtigt weiterhin nicht dazu. Danach bestand die gesamte Datei einschließlich abgelaufenem akzeptiertem Trade und verweigertem Restore. Zusätzliche adversariale Journalfälle werden noch ergänzt; Übergabe: [002-PROTECTED-ENTRY-RECOVERY-NOTES.md](002-PROTECTED-ENTRY-RECOVERY-NOTES.md).
- Nach diesen Änderungen erneut: eigener ESLint und Backend-Typecheck grün. Ein paralleler Architektur-/Komplexitätslauf fand Zwischenfehler im noch bearbeiteten Fillidentity-Paket; an dessen Besitzer gemeldet, keine Gates gesenkt. Der gemeinsame Gesamtlauf wird erst nach dessen Integration wiederholt. Schema40 ist durch den früheren Schema39-Gesamtlauf ausdrücklich nicht zertifiziert.
- Fremdwährungsbewertung benötigt noch die angefragte Nutzerentscheidung zur maximal zehn Sekunden alten vorherigen Kursbeobachtung; keine Zustimmung unterstellt. [005-FX-FOLLOWUP-PLAN.md](005-FX-FOLLOWUP-PLAN.md) ist Entwurf, kein implementierter Bewertungsvertrag. Kraken-Protected-IOC wird nochmals anhand öffentlicher Primärquellen geklärt. 009 und abschließende 008 bleiben vollständig Bestandteil des Ziels.

## Anschließender bestätigter Recovery-Integrationsstand

- Der Originalrequest-Vertrag aus Candidate-Admission ist nach `trading_plan_identity.ts` extrahiert und schützt jetzt ebenso Recovery/Abandon. Sechs beschädigte Originalbindungsvarianten (einschließlich geänderter Requestmenge mit neu korrektem Hash) bleiben ungeklärt, ohne lokalen Abschluss oder zweiten Send. Der private aktuelle Dispatch-Witness bleibt zusätzlich erforderlich; keine alte Operation-ID bekommt diese Berechtigung.
- Weitere echte rote Workerfälle sind repariert: Ein gültig vorbereiteter, nachweislich nie gesendeter Originalplan wird bei entzogener Runtime-, Konto- oder Routenberechtigung auch während `startProtectionOnly` und periodischer Überwachung lokal terminalisiert. Dieser neue separate Recoveryzweig ruft keinen Adapter und keine neue Entryfreigabe auf. Bereits angenommene offene Entries samt Stop bleiben beim bloßen Execution-Aus unverändert. Insgesamt **28 echte Child-Crash-Szenarien** sowie die zusätzliche Already-Sent-Worker-Gegenprobe grün.
- Neue registrierte `test_trading_preparation_recovery.js`: Eine ausdrücklich unvollständige importierte 100-Zeilen-Altseite darf spätere beweisbar widerrufene Vorbereitungen nicht dauerhaft verdrängen. Zuerst rot, danach faire rotierende begrenzte Auswahl grün. Beschädigte Originalpläne bleiben als Fehler erhalten, unterbrechen aber nicht die andere sichere lokale Arbeit. Kein Löschen der Altzeilen, kein Provideraufruf und keine künstliche Freigabe.
- Dispatch-Fence-Test erweitert: **44** Callbackmutationen von Request, verschachtelter Menge, Konto-/Credentialbindung, Intent, Operationsart und Leg-ID erzeugen keinen Send; ursprüngliche Journalfelder bleiben unverändert. Prüfung auch ohne optionalen `beforeSend`-Callback grün.
- Letzter Root-Nachlauf **8/8 Dateien grün**: PreparationRecovery, ProtectedEntryCrash, RecoveryWorker, Emergency, GlobalRelease, EntrySafety, ProtectionReceipt, StartupTrading. Der zwischenzeitliche Emergency-Fillnamespace-Fixturekonflikt ist mit nachgewiesenen synthetischen Provideroriginalen korrigiert, ohne Ownershipgate zu lockern. Backend-Typecheck, eigene ESLint-Dateien, Diffcheck, Architektur (145 Module / 622 Imports / keine Zyklen) und vollständiges Komplexitätsgate (0 Warnungen, Maximum15) waren in diesem Stand grün.
- Kraken-Batch-Primärklärung abgeschlossen, aber Quellenvertrag weiterhin nicht beweisbar: [004-KRAKEN-BATCH-RESEARCH.md](004-KRAKEN-BATCH-RESEARCH.md). Serialisierbarer SDK-Body genügt nicht, den widersprüchlichen Stopvertrag freizugeben. Kein Entry-Cap auf Stop und kein Ersatz-Einzelentry eingeführt.
- Migration40/Order-/Fillnamespace wird noch abschließend integriert; insbesondere additive belegte Altfillbindung und ihr gemeinsamer Safety-Status werden ergänzt. Unbewiesene Legacydaten dürfen weder still grün werden noch trotz vollständiger Originale künstlich dauerhaft gesperrt bleiben. Zwei neue Python-Produktionsmodule sind im Sonar-Quellscope enthalten. Kraken-Cashleg-/Fee-Korrelation ist der nächste abgegrenzte005-Teil; keine FX-Policy vorweggenommen. Dies ist weiterhin kein vollständiger aktueller Gesamtlauf und keine Freigabe des Gesamtziels.

## Folgelauf: ursprüngliche Geldbelege und erneute Ingress-/Betriebsprüfung

- Root hat den [Kraken-Cashleg-Vertrag](005-KRAKEN-CASHLEG-NOTES.md), bestehende Ledger-/Consumerimplementierung und die offiziellen Kraken-Execution-/Accountlogquellen erneut gelesen. Die enge native Ergänzung ist zur Umsetzung freigegeben; additive Migration41 ist dafür reserviert und db.ts/Ledger wurden vom Migration40-Besitzer ausdrücklich übergeben. Eingaben der Bewertungs-API sind ausschließlich ursprüngliche Ereignis-/Receiptverweise. Keine vom Aufrufer gesetzte Gebührenwährung, kein nachträgliches Überschreiben von Fill-/Moneyoriginalen, keine neue Funding-ID und keine allgemeine FX-/As-of-Freigabe.
- Zwei neue echte rote Regressionen sind als Ausgangspunkt bestätigt: Ein eigener belegter Kraken-Fill mit eindeutigem USD-Cashleg bleibt bislang unbewertet; ein originales Konversionsprozentfeld geht im Producer verloren. Der positive Korrekturbeleg und alle Crosspage-/Restart-/Konfliktregressionen stehen noch aus. Ein synthetischer nativer USD-Fall wird nicht als echte Providerabnahme ausgegeben.
- Unabhängige Legacyreview fand einen relevanten Bybit-Scope-Gegenbeleg: Auch Optionen können im gepinnten SDK `linear=true` tragen. Dieses Accountingfeld allein beweist daher keinen linearen Perpetual-Fillnamespace. Der ursprüngliche Produkt-/Orderbeleg bleibt erforderlich. Die laufende additive Altbindung wird zusätzlich auf tatsächlich begrenzte, faire Verarbeitung ungeklärter Altzeilen geprüft; ein Limit nur erfolgreicher Änderungen wäre kein Lesebudget.
- Erneut unter Node22.23.2 bestätigt: **15/15** fokussierte Dateien zu Ingress/AI/Signalidempotenz, Startup-HTTP, echten DB-Wartungs-Acks, harten Backupabbrüchen, Backupnachweisen und Migrationsrecovery. Danach **13/13** zu Crash-/Prozessownership, MCP-Wartung/Controlplane, Backup/Offsite/Generation, Retention, Outboxscheduler, Workflowbuilder/-historie und Metriken. Ausschließlich temporäre DBs, lokale Fakes und isolierte Prozesse; keine echte Anwendung oder entfernte Konten. Die Windows-Verzeichnis-Sync-Probe meldet weiterhin EPERM: daraus folgt ausdrücklich kein Power-Loss-Durabilitätsnachweis.
- Diese 28 grünen Dateien sind zusätzliche Teilnachweise, keine aktuelle vollständige Suite nach Migration41, keine Abschlussbehauptung für001–009 und kein Sonar-/Provider-/Releasebeleg. Reihenfolge001–007 →009 →008 und gesamter Zielumfang bleiben unverändert.

## Bestätigter Nachlauf der additiven Altbindung und Qualitätswerkzeuge

- Root hat die neue `test_trading_fill_identity_backfill.js` direkt unter Node22 erfolgreich ausgeführt. Der erste Aufruf über den gemeinsamen Runner war noch wegen fehlender Registrierung abgewiesen worden und zählt nicht als Testlauf; die Registrierung ist anschließend erfolgt. Zusätzlich **9/9** registrierte Dateien grün: FillIdentity, ExchangeFillIdentity, NativeOrderBindings, OriginalRequestTags, PreparationRecovery, ProtectedEntryCrash, RecoveryWorker, EntrySafety und ProtectionReceipt. Damit sind die echten bisherigen Crash-/No-Send-/Schutzregressionen auch nach der neuen Altbindung erneut belegt.
- Der Root-Review präzisierte zwei relevante Quellenfences: widersprechende originale Bybit-Kategorie nicht durch ein syntaktisch passendes Symbol überstimmen; neue Fill-Identitätsfelder im Risikobeleg mitführen. Der verantwortliche Agent hat zu beiden Punkten rote→grüne Regressionen ergänzt. Die neue faire Altbindung begrenzt tatsächlich untersuchte Datensätze; kontoweite finale Unsicherheit wird weiterhin unabhängig und vollständig bestimmt.
- Qualitätswerkzeuge separat erneut **6/6** grün: Sonar-Leseexport, Sonar-Evidenzgate, synthetischer Exchange-Acceptancevalidator, Releaseartefakte, Repositorygovernance und Supply-Chain-Pins. Dies ist ausdrücklich kein Abruf eines neuen echten Sonar-/CI-Gates. Der lokale Acceptance-Runner erhält gerade sein bisher fehlendes dauerhaftes Cleanupjournal; reale Ausführung bleibt nicht autorisiert und nicht vorgenommen.
- Die Python-Coverage-Werkzeuglücke ist lokal behoben, ohne TSX-Abhängigkeiten zu verändern: separate Umgebung, exakt gepinnte Coverage-Version und verifiziertes Windows-Wheel. Tatsächlicher Smoke-Lauf mit zehn Tests und messbarem Bericht; vollständige Coverage steht weiter aus. Details und getrennte Nachweisgrenzen in [008-IMPLEMENTATION-NOTES.md](008-IMPLEMENTATION-NOTES.md).
- Zwischenzeitlich entdeckter Importzyklus Ledger↔Cashlegrepository ist aufgelöst. Letzter Root-Typecheck und Architekturprüfung grün (149Module/646interneImports/0Zyklen); vorheriger vollständiger Komplexitätslauf ebenfalls ohne Warnungen/max15. Die gerade entstehende Cashlegintegration und Migration41 werden erst nach ihren übrigen positiven/negativen Tests gemeinsam abgenommen.

## Weiterer integrierter Nachlauf: Schema41, echte Simulatorbelege und verbleibende Fristlücke

- Die native Kraken-Cashleg-Ergänzung ist nach dem 19-Dateien-Fokuslauf übergeben; Root hat die vollständige aktualisierte Übergabe gelesen. Der anschließende gemeinsame JS-Nachlauf besteht die vier Cashlegdateien einschließlich tatsächlicher 40→41-Migration, unveränderter Originale und Konflikt-/Restartfälle. Der Gesamtauftrag005 bleibt wegen der dokumentierten Einheiten-/FX-Restarbeit offen.
- Vollständiger Pythonlauf mit dem neuen Acceptance-Journal: **241 Tests grün**. Gemessene Coverage90% im bestehenden CI-Scope bzw. separat83% ausschließlich für den produktiven Executor; beide bestehen die unveränderte60%-Schwelle. Details: [008-IMPLEMENTATION-NOTES.md](008-IMPLEMENTATION-NOTES.md). Dieser Zwischenstand ist kein echter Providertest und liegt vor weiteren laufenden Korrekturen.
- Die erneuten vollen JS-Läufe fanden drei alte positive Fixtures, die Fillzeilen ohne unabhängige Originale erzeugten. `test_trading_entry_commitment.js` nutzt jetzt wirkliche Paper-Entries/Stops und persistierte Originalfills. `test_trading_global_release.js` bestätigt zusätzlich explizit die Ablehnung erfundener historischer Rundtrips und die Freigabe eines echten abgeschlossenen Simulatortrades. `test_trading_failures.js` verwendet für die Daily-Risk-Grenze einen tatsächlich ausgeführten Verlusttrade (0,327 × (2800 − 3050) = −81,75), nicht ein frei gesetztes Positions-PnL. Alle drei Dateien einzeln grün; Schutzgates unverändert. Die Viewer-Schemaassertion wurde auf den tatsächlichen Migrationsstand41 aktualisiert, keine Migration ausgelassen.
- Ein neues Registrierungs-Vorgate verhindert, dass vorhandene JS-Testdateien im vollständigen Runner unbemerkt fehlen; eigene positive/negative Dateisystemtests grün. Die bislang fehlenden Fill-/Cashlegtests sind registriert. Ein vollständiger abschließender JS-Nachlauf ist nach den gefundenen Fixtures noch erforderlich; Teilfortschritte werden nicht zusammengezählt und als voller neuer Lauf ausgegeben.
- Der unabhängige004-Vertragsabgleich fand außerdem eine echte verbleibende Produktionslücke: Die unveränderliche ursprüngliche Entryfrist erreicht den Sidecar bislang nicht. Zwei kontrollierte Fakes beweisen einen Batchversand nach Ablauf während Mutation-Wait oder Modusread. Der enge Transport-/finale Versandfence-Fix ist zur Umsetzung freigegeben; keine neue Frist, keine Behinderung späterer Schutz-/Cancelaktionen, kein Fallback nach möglicherweise begonnenem Send. Diese offene Korrektur verhindert eine Abschlussbehauptung für004.
- Weiterhin ausschließlich lokale Arbeitskopie, temporäre Datenbanken/Fakes und öffentliche Primärquellen. Keine TSX-Server-/Konto-/Appverbindung, echte Trades, Commits oder Veröffentlichung. Reihenfolge001–007→009→008 und vollständiger Zielumfang bleiben erhalten.
